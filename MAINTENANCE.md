# Nib 유지보수 노트

**모르면 반드시 실수하는 것들**만 모았습니다. 구조와 실측값은 [ARCHITECTURE.md](ARCHITECTURE.md),
사용법과 배포는 [README.md](README.md).

---

## 개발 환경

문법 검사는 node(v24)로 합니다.

```bash
for f in *.js; do node --check "$f"; done
```

`sidepanel.js`는 ES 모듈이지만 `--check`는 통과합니다. 파싱만 보는 것이라 괜찮습니다.

**UI 검증은 브라우저에서, 눈이 아니라 자로.** `chrome.*`를 스텁으로 채운 미리보기 페이지를 띄우고
`getBoundingClientRect` / `getComputedStyle`로 재는 편이 정확합니다. 실제로 이 방법으로만 찾은
버그가 여럿입니다(마이너스 버튼 정렬, 아코디언 눌림).

**전환 중인 속성은 프레임이 안 도는 환경에서 얼어붙습니다.** 화면을 그리지 않는 브라우저에서
테마를 바꾸고 색을 재면 `transition`이 걸린 속성만 옛 값이 남아 "테마가 안 먹는다"로 보입니다.
잴 때는 `* { transition: none !important }`를 끼워 넣으세요.

**요소 참조를 다시 그리기 전에 잡아두지 말 것.** 떼어진 노드의 `getComputedStyle`은 빈 문자열을
돌려줘서 "스타일이 안 먹었다"로 오독하게 됩니다. 다시 그린 뒤 새로 질의하세요.

---

## 한 곳을 고치면 함께 고쳐야 하는 것

### `shared-ui.js`는 항상 맨 앞

manifest의 content_scripts js 배열에서 **반드시 첫 번째**여야 합니다.
`overlay.js` · `editor.js` · `queue.js` · `cleaner.js`가 모두 로드 시점에 `NibUI`를 읽기 때문입니다
(`shellCSS()`, 그리고 `normalizeSlots`를 지역 이름에 묶는 줄).
`viewer.js`는 아무에게도 안 기대므로 맨 뒤입니다 — 이 파일이 죽어도 삽입 엔진은 살아야 합니다.

**새 창 파일을 만들면 manifest만 고치면 됩니다.** `sidepanel.js`의 `send()`는 주입 목록을
`chrome.runtime.getManifest()`에서 그대로 읽습니다 — 목록이 두 벌이 되지 않게 일부러 그렇게 두었습니다.

### 콘텐츠 스크립트는 전부 IIFE 안에 있어야 합니다

`send()`의 그 주입 경로는 **이미 실행된 페이지에도 다시 주입합니다.** 콘텐츠 스크립트는 하나의
격리 world를 나눠 쓰므로, 최상위 `const`가 IIFE 밖에 있으면 두 번째 주입이
`Identifier 'SEL' has already been declared`로 통째로 실패합니다 — 그 뒤 파일까지 다 같이 죽습니다.

밖에 내놓는 이름은 파일마다 하나씩입니다.

```
shared-ui.js  globalThis.NibUI
content.js    globalThis.NibEngine
overlay.js    globalThis.toggleOverlay · globalThis.NibUsage
editor.js     globalThis.openPresetEditor
queue.js      globalThis.openQueue
cleaner.js    globalThis.openCleaner   (순수 엔진은 openCleaner.convert 로 그 위에 매단다)
viewer.js     (없음)
```

### 두 곳에 있는 것 — 컨텍스트가 다르면 어쩔 수 없습니다

같은 것을 두 벌 두는 것은 **서로의 파일을 못 읽을 때뿐**입니다 — 사이드 패널과 콘텐츠 스크립트,
그리고 MAIN world의 page.js. 콘텐츠 스크립트끼리는 `NibUI` / `NibEngine` / `NibUsage`로 나눠 쓰므로
**사본을 만들지 마세요.**

| 무엇 | 어디에 | 왜 |
|---|---|---|
| `splitTags` · `normalizeTag` | content.js · sidepanel.js | 실행 컨텍스트가 다르다 |
| `normalizeSlots` | shared-ui.js · sidepanel.js | 〃 (편집 창·예약 창은 shared-ui 것을 쓴다) |
| `esc` | shared-ui.js · sidepanel.js | 〃 (이름도 본문도 일부러 글자까지 같다) |
| `uid` | sidepanel.js · editor.js | 〃 — 둘 다 프리셋 id를 만든다 |
| `FREE_PIXELS` · `FREE_STEPS` | page.js · queue.js | page.js만 MAIN world라 서로를 못 읽는다 |
| `RECORD_MODEL` / `SUPPORTED_MODEL` | page.js · queue.js | 〃 — 같은 정규식이어야 완료 신호가 온다 |

**같은 저장 키는 같은 이름으로 부릅니다.** `nib.library`는 세 파일이 읽고 쓰는데
한때 `STORE_KEY`와 `LIB_KEY` 두 이름으로 불렸습니다 — 그러면 상수 이름으로 grep해도
읽는 쪽이 다 안 잡힙니다. 지금은 셋 다 `LIB_KEY`입니다.

**한쪽만 고치면 "활성으로 보이는데 눌러도 안 빠진다"가 됩니다.**
무료 판정 상수는 더 조용합니다 — 한쪽만 고치면 예약 창이 "무료"라고 적어 둔 채로 돈이 나갑니다.
모델 정규식이 갈라지면 큐가 완료 신호를 못 받아 워치독까지 기다렸다 멈춥니다.

**이 표에 없는 것은 사본을 두면 안 됩니다.** 한때 `OPUS_MODEL`이 overlay.js와 queue.js에
따로 있었는데, 둘은 같은 격리 world라 나눌 수 있었습니다. 지금은 overlay.js 한 곳에만 두고
`NibUsage.usesOpus(model)`로 내보냅니다. 장당 평균의 "±10%까지 몇 장 더"도 같은 이유로
`analyze()`의 `toTarget` 한 곳에서만 계산합니다.

### 출력 뷰어 — 카메라 transform은 절대 덮지 말 것

V5 출력부는 무한 캔버스입니다. `.image-gen-canvas` 바로 밑의 **클래스 없는 div**가 카메라이고,
`transform: matrix(…, 522, 53.11)`이 JS로 인라인에 찍힙니다.

```css
/* 이렇게 하면 애니메이션이 아니라 카메라가 죽는다 */
.image-gen-canvas * { transform: none !important; }
```

애니메이션만 멎는 게 아니라 **좌표가 통째로 0이 되어 이미지가 화면 밖에 남습니다.**
증상이 "안 보인다"라서 CSS를 의심하지 않게 됩니다.

`viewer.js`가 `image-grid-*` 클래스만 잡는 것은 그래서입니다 —
카메라 div는 클래스가 없으므로 **구조상 걸릴 수가 없습니다.** 선택자를 넓히지 마세요.

그리고 **스크롤 애니메이션·패닝·줌은 여기서 흉내내지 마세요.** NovelAI에 이미 설정이 있습니다
(`simpleOutputViewer` / `canvasReducedMotion` / `canvasLockCamera`). 자세한 건 ARCHITECTURE.

### `splitTags`의 구분자는 쉼표와 줄바꿈 둘 다입니다

`/[,\n]/`입니다. 위의 〈두 곳에 있는 것〉 표대로 `content.js`(넣고 빼기)와 `sidepanel.js`(활성 표시·검색)에
따로 있으니 **한쪽만 고치면 "활성으로 보이는데 눌러도 안 빠진다"가 됩니다.**

같은 이유로 `content.js`의 `readEditor`는 **줄바꿈을 공백으로 바꾸지 않습니다.**
바꾸면 줄 끝 태그가 다음 줄 첫 태그와 붙어 프리셋이 활성으로 안 잡힙니다.
같은 줄의 `\u00a0`는 ProseMirror가 넣는 nbsp입니다 — 소스에서 보통 공백과 구분이 안 되니
정규식에 글자를 직접 넣지 말고 **이스케이프로** 두세요.

넣고 뺄 때는 `splitTags`가 아니라 `splitParts`/`joinParts`를 씁니다 — 구분자를 짝으로 들어야
여러 줄 프롬프트가 한 줄로 뭉개지지 않습니다. 자세한 건 ARCHITECTURE.

### 태그 정리기는 `\s`를 쓰면 안 됩니다

`cleaner.js`의 공백 정리는 `[^\S\n]`과 `[ ]`만 씁니다. `\s`를 쓰는 순간 줄바꿈까지 먹어
여러 줄로 짠 프롬프트가 한 줄로 뭉개집니다 — 바로 위 `splitTags` 항목과 같은 함정입니다.

**닫는 `::` 앞의 공백은 앞 글자가 숫자면 지우면 안 됩니다.** `hp 5 ::`를 `hp 5::`로 만들면
다음 실행에서 `5`가 여는 가중치로 읽혀 **한 번 더 돌렸다고 결과가 달라집니다.**
그래서 `([^\d. ])[ ]+::` 로 앞 글자를 보고 판단합니다.

**멱등성이 합격 기준입니다.** 두 번 돌려서 `1.05::1.05::tag::::`가 나오면 실패입니다.
`{{{tag}}}`는 1.15가 아니라 **1.16**입니다(1.05³ = 1.157625). 표가 아니라 식을 쓰세요.

**`:숫자`가 있을 때만 괄호를 건드립니다.** 맨 괄호까지 잡으면
`hakurei reimu (touhou)`의 시리즈명이 가중치가 됩니다. 근거는 ARCHITECTURE의 "태그 정리기".

### 기록하는 모델과 집계하는 모델은 다릅니다

```
page.js    RECORD_MODEL      V4.5 · V5   ← Anlas가 나가고, 예약 큐가 완료 신호로 쓴다
queue.js   SUPPORTED_MODEL   V4.5 · V5   ← 〃 (world가 달라 어쩔 수 없는 사본)
overlay.js OPUS_MODEL        V5 뿐        ← percent를 깎는 것은 이것뿐 (실측)
                                            queue.js는 NibUsage.usesOpus 로 이것을 빌려 쓴다
```

**두 범위를 한 곳으로 합치지 마세요.** 합치면 둘 중 하나가 반드시 틀립니다 — V4.5를 빼면 큐가
완료 신호를 못 받아 워치독까지 멈춰 서고, V4.5를 넣으면 장당 평균이 통째로 망가집니다.

반대로 **같은 범위를 두 벌 두지도 마세요.** `usesOpus`는 판정에 실패해도 `false`를 돌려주므로,
"V4.5라서 0%"보다 **먼저** 물으면 overlay.js가 안 실렸을 때 0%라고 단정해 버립니다
(`opusEstimate`의 검사 순서가 그래서 그 모양입니다).

### 과금 생성은 "빼는" 것이 아니라 "자르는" 것입니다

Anlas가 나간 V5 생성이 할당량도 함께 깎는지는 **관측할 수 없습니다.** 그래서
`paidGen`은 분모에서 한 장을 빼는 것이 아니라 `paidUntil`로 **그 이전 구간을 버립니다.**

분모에서 빼는 방식으로 "간단하게" 되돌리지 마세요. 그 생성이 실제로 할당량을 깎았다면
그 하락이 남은 무료 생성들에 얹혀 **장당 평균이 조용히 커집니다.**
V4.5를 분모에서 빼는 것(`countsForOpus`)과는 다른 문제입니다 — 그쪽은 안 깎는 것이
확인됐으니 빼도 됩니다.

### 마크는 퍼센트가 바뀔 때만 생깁니다

1%p ≈ 17장이라 **몇 장을 뽑아도 마크가 안 생기는 구간이 깁니다.**
"다음 마크가 올 때까지 기다린다"는 조건을 걸면 그 사이 영원히 안 풀립니다 —
`paidUntil`이 그렇게 만들어져 경고가 붙박이가 된 적이 있습니다.

**마크를 기다리는 로직에는 반드시 대체 경로를 두세요.**

### 숫자를 감추지 마세요

표본이 모자랄 때 값을 `표본 부족`으로 갈아끼우면 기능이 고장난 것처럼 보입니다.
**숫자는 늘 띄우고, 왜 못 믿는지를 그 아래 작은 글씨로 적습니다.**
`analyze()`가 `paidMixed` · `paidUnsettled` · `clamped` · `reliable` · `toTarget`을 돌려주는 것은
그 문장을 조립하라고 있는 것입니다.

**다만 없는 숫자를 지어내지도 마세요.** 회복이 소모보다 큰 구간에서는 장당 평균이 0 이하라
"몇 장 더"가 나오지 않습니다. `toTarget`은 그럴 때 `null`입니다 — 그 자리는 숫자가 아니라
말로 바꿔야 합니다. 예전에는 `Infinity장`이 그대로 찍혔습니다.

### Anlas `cost`의 `0`과 `null`은 다릅니다

`0`은 "무료가 맞고 실제로 안 깎였다", `null`은 "못 쟀다"입니다.
`null`을 0으로 접어 넣으면 합계가 조용히 틀어집니다. 표에서도 `0`과 `—`로 갈라 씁니다.

### 예약 큐가 붙잡고 있는 것

- **Generate 버튼은 두 벌입니다**(모바일+데스크톱). 클래스는 `image-gen-generate-button`이고
  안 보이는 쪽을 누르면 아무 일도 안 일어납니다. `visible()`을 반드시 거치세요.
  완료 판정에 `disabled`만 쓰면 안 됩니다 — NovelAI가 버튼을 취소 버튼으로 바꿔치면
  **취소를 눌러버립니다.** 첫 낱말이 시작 시점과 같아졌는지까지 봅니다.
- **장수는 클릭한 뒤 `localStorage`로 되읽어 확인합니다.** 눌렀다고 바뀐 것이 아닙니다.
- **스크롤바는 `*` 선택자로 한 번에 줍니다.** 특정 영역에만 주면 나머지가 따로 놉니다.
  `color-scheme`도 같이 있어야 합니다 — 없으면 어두운 테마에서 스크롤바만 하얗게 뜹니다.
  shadow DOM은 `all: initial`이 상속을 끊으므로 `:host`에 **따로** 적어야 합니다.
  그 `*` 규칙은 투명 테두리 + `background-clip: padding-box`라 **어떤 바탕 위에도 그대로 앉습니다** —
  영역마다 바탕색을 맞춘 덧규칙을 다시 달지 마세요. 하나 예외는 편집 창의 `.emoji-grid`인데,
  거기만 폭이 8px입니다(판 너비 376px을 그 값에서 거꾸로 잡았습니다).
- **`BATCH_MAX`를 1보다 크게 올리지 마세요.** Opus 무료는 요청당 한 장까지라,
  4장을 한 번에 요청하면 나머지 3장이 과금됩니다(실측 −60 Anlas). 요청 수가 주는 것은
  이득처럼 보이지만 **돈이 나갑니다.**
- `queue.js`는 `globalThis.NibEngine`(content.js)과 `globalThis.NibUsage`(overlay.js)만 씁니다.
  콘텐츠 스크립트는 한 컨텍스트를 공유해 그냥 불러도 되지만, 그러면 파일 사이 의존이
  소스 어디에도 안 적힙니다. **이 두 이름 밖의 것은 쓰지 마세요.**
  `NibUsage`에서 빌려 쓰는 것은 `analyze`와 `usesOpus` 둘입니다.
- **`무료 구간` 표시는 크기·스텝만 봅니다**(`isFreeSize`). page.js의 `expectFree`와 달리
  캐릭터 레퍼런스는 못 봅니다 — 그 값은 요청 본문에만 있고 얼려둔 베이스에는 없습니다.
  즉 레퍼런스를 붙이면 `무료 구간`이라고 적혀 있어도 Anlas가 나갑니다.
  진짜 안전판은 잔액을 되읽는 `guardStop`이지 이 표시가 아닙니다.
- **취소는 `cancelRun()` 한 곳입니다.** 시작 버튼과 닫기 두 길이 들어오는데, 갈라 두면
  한쪽만 다시 그려서 "닫았는데 아무 말도 없다"가 됩니다.
- 세트 전환은 **`remove`가 아니라 `replace`+`add`**입니다. 이유는 ARCHITECTURE.
- NovelAI 설정값(`imageAutoDownload` 등)은 **파이버로 못 읽습니다.** 다시 시도하지 마세요 —
  jotai 모듈 스코프라 566ms를 태우고 실패합니다.

### `[hidden]`에는 `!important`가 필요합니다

```css
[hidden] { display: none !important; }
```

`.field { display: block }` 같은 작성자 규칙이 브라우저 기본 `[hidden]`을 이깁니다.
이것 때문에 편집 창의 세 칸이 항상 동시에 보였고, 그 상태에서 저장하면 프리셋이 의도치 않게
칸별 모드가 되어 **캐릭터 선택창을 건너뛰는** 2차 증상까지 생겼습니다. 한 버그가 두 증상을 만듭니다.

**`sidepanel.css`와 `shared-ui.js`의 `shellCSS()` 양쪽에** 있어야 합니다.
shadow DOM은 바깥 CSS를 받지 않으므로, 패널에만 적으면 편집 창의 감춘 textarea가 그대로 다 보입니다.

### 아이콘은 네 곳

```
icons/nib.svg                 ← 원본
icons/icon{16,32,48,128}.png  ← 여기서 래스터라이즈
sidepanel.html  .mark
shared-ui.js    markSVG()     ← 사용량 창·편집 창·예약 창·정리 창이 함께 쓴다
```

**mask id는 창마다 달라야 합니다.** 같은 id가 문서에 둘이면 나중 것이 앞의 것을 가려 마크가
통째로 사라집니다. 그래서 `markSVG('ov')` / `markSVG('ed')` / `markSVG('qu')` / `markSVG('cl')`로
접미사를 받습니다.

- **CSS로 `border-radius`를 다시 주지 말 것** — 타일이 이미 둥글어 이중으로 깎이고 툴바 아이콘과
  달라집니다. 그림자는 `filter: drop-shadow`.
- **SVG에 테두리를 굽지 말 것** — 밝은 배경에서 테처럼 보이고 클리핑과 겹쳐 잘립니다.

### 라이브러리를 쓸 때 items만 갈아끼울 것

```js
// editor.js — 이렇게 써야 한다
await chrome.storage.local.set({ [LIB_KEY]: { ...lib, version: lib.version || 2, items: next } });
```

`{ version, items }`만 써버리면 사이드 패널이 만든 폴더가 **조용히** 사라집니다.
에러도 안 나고, 다음에 패널을 열었을 때 폴더만 없어져 있습니다.
편집 창은 프리셋만 다루지만 저장은 라이브러리 통째로 하므로, 이 함수만은 폴더를 알아야 합니다.

**자동 백업도 같은 함정을 밟았습니다.** `backupToFolder()`가 `{ version: 1, items }`만 쓰던 시절이
있었고, 그 파일로 복구하면 폴더가 통째로 사라졌습니다. 지금은 내보내기와 같은 `libraryPayload()`를
씁니다 — 한 곳만 고치면 됩니다.

---

## 태그 삽입

### `toggle` 판정은 손대지 않은 값으로

자동 성별 태그를 걷어낸 목록으로 넣기/빼기를 판정하면, `girl`만 든 칸에서 **빼기가 넣기로 뒤집힙니다.**
`cleanGender`는 넣기 경로에만 끼우고, `allPresent` 계산은 원본 `current`로 하세요.

걷어내는 조건을 좁게 잡은 것도 의도입니다(캐릭터 프롬프트 칸 + 넣는 태그에 성별이 있을 때 +
낱개 `girl`/`boy`만). 넓히면 "화풍만 넣었는데 성별이 사라졌다"가 됩니다. 자세한 이유는 ARCHITECTURE.

### 칸별 프리셋

- `charUCs`와 `chars`는 짝을 이룹니다. 칸을 지울 때 **둘 다** 같은 자리에서 `splice`해야 합니다 —
  하나만 지우면 그 뒤 캐릭터들의 UC가 통째로 한 칸씩 밀립니다.
- 편집 창에서 값을 다시 읽을 때 뒤쪽 빈 칸을 자르면 "칸 추가"가 제자리걸음합니다 —
  자르기는 **저장할 때만** (`collect(trim)`).
- 자를 때는 **프롬프트와 UC가 둘 다 빈** 칸만 버립니다. UC만 채운 칸을 프롬프트가 비었다고 버리면
  내용이 조용히 사라집니다.
- 캐릭터 칸은 펼치고 탭을 바꾸는 조작이 끼므로 **순차 적용**해야 합니다.

### 캐릭터 옮기기

넣기가 **성공한 뒤에만** 원래 칸을 비웁니다. 실패했는데 지우면 태그를 잃습니다.
그냥 `toggle`로 넣기만 하면 원래 칸에 남아 복제됩니다.

---

## UI

### 폴더

- 폴더를 지울 때 **안의 프리셋을 어떻게 할지 먼저 묻습니다**(`deleteFolder` → `dropFolder`).
  남기는 쪽은 `folderId`만 떼고, 지우는 쪽은 `confirm`을 한 번 더 받습니다.
  **세는 것은 화면에 보이는 카드가 아니라 `folderId`가 그 폴더인 프리셋 전부입니다** —
  화풍 탭에서 지워도 그 폴더의 씬 프리셋이 함께 걸립니다. 보이는 수를 적으면 거짓말이 됩니다.
- 프리셋을 지우는 갈래에서는 `renderList()`만으로 부족합니다. **`renderCats()`도 같이 불러야**
  분류 칩의 개수가 따라 줄어듭니다. 안 부르면 "40"이 그대로 남아 목록과 어긋납니다.
- 카드가 안 보이는 폴더로는 끌어다 놓을 수 없습니다. 그래서 **옮기기 창(`openMovePicker`)에는
  탭과 무관하게 모든 폴더를 나열합니다.** 이 창이 없으면 다른 탭의 폴더에는 넣을 방법이 없습니다.
- 검색 중 걸린 폴더는 펼쳐 보여주되 **저장된 `open`은 건드리지 않습니다** — 검색 한 번에 접어둔
  폴더가 전부 펴져 있으면 곤란합니다. `makeFolderRow(folder, children, forceOpen)`의 셋째 인자입니다.
- 빈 폴더 안내 문구는 두 가지입니다. 정말 비었을 때와, **들어는 있는데 지금 조건에 안 걸릴 때.**
  후자를 "비어 있습니다"라고 쓰면 거짓말이 됩니다.

### 끌어다 놓기

`dragenter` / `dragleave`는 **자식 요소를 지날 때마다 번갈아 터집니다.** 그냥 `dragleave`에서
강조를 지우면 폴더 안 카드 위를 지나는 순간 깜빡입니다. 깊이를 세서(`depth++/--`) 0이 될 때만 지웁니다.

- `dataTransfer.setData('text/plain', …)`를 꼭 넣습니다. 없으면 일부 환경에서 끌기가 시작조차 안 됩니다.
- 카드 몸통이 `<button>`입니다. 폼 컨트롤이 끌기 몸짓을 삼키는 경우가 있어 **몸통에도 `draggable`을** 줬습니다.
- `폴더 밖으로 빼기` 띠는 **폴더 안 카드를 끌 때만** 나옵니다(`body.dragging-from-folder`).
  늘 띄워두면 목록 맨 위 한 줄을 영구히 잡아먹습니다.
- 넣은 폴더가 접혀 있으면 펴줍니다. 안 그러면 방금 넣은 것이 어디로 갔는지 보이지 않습니다.

### 창은 세운 뒤에 다시 재서 화면 안으로 밀어 넣어야 합니다

창 높이는 `max-height: calc(100vh - 40px)`까지 자랍니다. 자리를 잡는 시점에는 본문이 비어 있어
창이 낮으므로, 그때 계산한 위치는 내용이 채워지면 아래가 화면 밖으로 나갑니다.
하필 거기 있는 게 **저장 버튼**입니다.

그래서 `NibUI.clampIntoView()`를 내용이 바뀐 뒤에도 부릅니다 — `overlay.js`는 `render()` 끝에서,
`editor.js`는 `renderSlots()` 끝에서. 창 크기 변경도 같은 함수가 받습니다.

### window에 건 이벤트는 스스로 떨어지게 합니다

두 창 모두 열 때마다 새로 만들어집니다. `mousemove` / `mouseup` / `resize`를 `window`에 걸어두고
그냥 두면 여닫을 때마다 쌓입니다. `win.isConnected`가 거짓이면 자기 자신을 `removeEventListener` 합니다.

### 불러오기 창 — flex 최소 크기 함정

두 칸(`.imp-scroll`)은 각자 안에서만 스크롤합니다. 시트가 통째로 스크롤하면 맨 아래
**[가져오기] 버튼이 화면 밖으로 밀립니다** — 편집 창 저장 버튼과 같은 함정입니다.
그래서 `dialog.wide > .sheet`는 세로 flex이고, `.imp-cols`는 `minmax(0, 1fr)` 행입니다.
`minmax(0, …)`가 아니면 그리드 항목이 자기 내용보다 작아지지 못해 시트를 뚫고 나갑니다.

**그리고 `.imp-scroll > * { flex: 0 0 auto }`가 반드시 있어야 합니다.**
flex 항목의 자동 최소 크기(`min-height: auto`)는 `overflow: visible`일 때만 걸립니다.
아코디언 껍데기는 모서리를 깎느라 `overflow: hidden`이라 최소 크기가 0이 되고, 이것이 없으면
**스크롤이 생기는 대신 내용이 눌려 잘립니다.** 실제로 그렇게 만들었다가 재서 찾았습니다 —
921px짜리 `details`가 134px로 눌려 있었고, 겉보기에는 그냥 "목록이 짧네"로 보입니다.

### 세로 flex 목록은 전부 `flex: 0 0 auto`가 필요합니다

`.imp-scroll` 함정(위)과 **정확히 같은 것이 카드 목록에도 있었습니다.**
`.list` / `.folder-body` / `.charpick-list`는 세로 flex이고 `.card`는 `overflow: hidden`이라
자동 최소 크기가 0입니다. 240장을 넣고 재니 카드가 **1.3px까지 눌려** 있었고,
`list.scrollHeight`가 1,316밖에 안 됐습니다(눌린 만큼 스크롤이 안 생긴 것). 지금은 전부 85.8px,
`scrollHeight` 19,693입니다.

```css
.list > *, .folder-body > *, .charpick-list > *, .imp-scroll > * { flex: 0 0 auto; }
```

**세로 flex에 스크롤을 붙일 때마다 이 규칙을 같이 넣으세요.** 증상이 "레이아웃이 깨진다"가 아니라
"항목이 조금 작아 보인다"라서 눈으로는 못 찾습니다 — `getBoundingClientRect().height`로 재야 보입니다.

### 이모지 그리드는 `repeat(10, 1fr)`이 아니라 `minmax(0, 1fr)`

`1fr`은 `minmax(auto, 1fr)`입니다. 최소 크기가 칸 내용에 끌려 올라가 열 열 개의 합이 폭을 넘기고,
그러면 `overflow-y: auto`가 가로에도 `auto`로 번져 **10번째가 가로 스크롤 너머로 밀립니다.**
`.emoji-panel`의 폭(376px)은 칸 32 × 10 + 틈 3 × 9 + 스크롤바 8 + 여백 18에서 거꾸로 잡은 값입니다.

- 이모지 글리프 폭은 글꼴마다 다르므로 `overflow-x: hidden`을 빗장으로 덧댑니다.
- 갈래마다 개수가 달라 스크롤바가 들락거리면 칸이 들썩입니다 — `scrollbar-gutter: stable`.
- **한 이모지는 한 갈래에만.** `checkEmojiSets()`가 판을 처음 펼칠 때 훑어 콘솔에 알립니다.
  갈래를 늘리거나 옮길 때 중복이 조용히 생깁니다.
- `EMOJI_SETS` 소스는 한 줄에 10개씩 적습니다. **소스의 한 줄 = 화면의 한 줄**이라야 눈으로 셀 수 있습니다.

### 불러오기 창을 다시 그릴 때

- **이름을 치는 동안 줄을 다시 그리지 말 것.** 포커스와 커서를 잃습니다. `input` 이벤트에서는
  이름표와 경고 상자만 제자리에서 고쳐 쓰고, 갈래가 바뀔 때만 통째로 그립니다.
- 왼쪽 아코디언은 **펴 둔 것을 기억해서** 다시 그립니다(`data-key`). 제외를 누를 때마다 접히면 못 씁니다.
- 겹치는 이름을 **정하지 않은 채로는 가져오기 버튼이 잠깁니다.** "같은 이름이면 알아서 합침"으로
  두면 조용히 남의 폴더에 남의 프리셋이 섞입니다.

### 사소하지만

- 숫자를 못 낼 때 `—`만 찍지 말 것. 고장난 것처럼 보입니다. "표본 부족"처럼 쓰고 왜인지 붙입니다.
- 칩 줄은 가로 스크롤 대신 `flex-wrap` — 잘린 항목을 못 보게 됩니다.
- 한 줄에 라벨·책갈피·버튼을 같이 놓을 때는 셋 다 **높이를 정해** `align-items: center`로 묶습니다.
  높이를 안 주면 각자 글자 상자 크기에 끌려다닙니다. 지금 편집 창의 칸 머리줄은 네 요소의 중심이
  **0.01px 안**에 듭니다 — 실측했습니다.
- 감춘 입력란의 값은 자바스크립트로 옮겨 담지 말고 **`hidden`으로 숨긴 채 DOM에 그대로** 둘 것.
  읽을 때 `querySelectorAll('[data-slot]')` 한 번이면 되고, "화면에 없는 값이 사라지는" 부류의
  버그가 아예 생기지 않습니다. 대신 **감춘 쪽에 내용이 있으면 표시(점)를 해야** 합니다.
- **옛 마이너스 버튼이 삐뚤어 보이던 원인은 끝내 재현하지 못했습니다.** 옛 CSS·옛 마크업을 격리해
  재보니 정확히 가운데였습니다(`display: grid`가 `svg`를 이미 블록화하므로 "svg가 인라인이라
  내려앉는다"는 설명은 **틀렸습니다**). 새 머리줄은 배경 있는 띠 안에 높이를 못박는 방식이라
  원인이 무엇이었든 같은 증상이 나올 수 없습니다. 고칠 때는 눈이 아니라 `getBoundingClientRect`로 재세요.
