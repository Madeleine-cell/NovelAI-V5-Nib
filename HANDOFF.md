# Nib 인계 — Opus 사용량 계산 (2026-09-23 기준)

이 문서 하나로 **지금 상태 · 믿어도 되는 것 · 아직 모르는 것 · 다시 재는 법**을 잡을 수 있게 썼습니다.
사용법은 [README.md](README.md), 내부 구조와 근거 표는 [ARCHITECTURE.md](ARCHITECTURE.md),
고칠 때의 함정은 [MAINTENANCE.md](MAINTENANCE.md), 작업 경위는 [WORKLOG-2026-09-23.md](WORKLOG-2026-09-23.md).

---

## 한 줄 요약

NovelAI V5의 Opus 할당량 소모를 **실측으로 맞춘 식**으로 환산해 평균을 내고, 실제 Chrome에서 확인했습니다.
구간 21개(무료 458장, 과금 103장이 섞임)에서 식의 예측이 스텝별 실측과 **±3% 안**입니다.

---

## 지금 상태 (v1.10.0)

| 항목 | 상태 |
|---|---|
| 사용량 창 계산 | 동작 · 실제 Chrome에서 파일 분석과 같은 값 확인 |
| 예약 창 Opus 예측 | 동작 · 베이스 크기·스텝으로 환산 |
| Anlas 차감 기록 | 동작 · 연속 과금 시 일부 짝이 어긋남(합계는 맞음, 아래 한계) |
| 기록 내보내기 | 동작 · 다운로드 폴더 `nib-ledger-YYYYMMDD-HHMMSS.json` |
| 문법 검사 | 전체 JS 통과 (`for f in *.js; do node --check "$f"; done`) |
| 백업 | `..\NovelAI V5 Nib.backup-2026-09-23` (이번 작업 전 원본) |

적용 순서: `chrome://extensions` → Nib ↻ → NovelAI 탭 F5. `background.js`가 바뀌면 ↻가 꼭 필요합니다.

---

## 소모 모델 — 무엇을 믿어도 되나

```
한 장 소모 ≈ (가로×세로 ÷ 832×1216) × (0.19 + 0.81 × 스텝/28) × 기준 1장당
기준 1장당 ≈ 0.066%p   (832×1216·28스텝 한 장, 원장에서 매번 다시 잰다)
회복       6048초 / 1%  (NovelAI가 주는 값, 0→100% 7일)
```

| 사실 | 근거 강도 | 근거 |
|---|---|---|
| **V5만** 할당량을 쓴다 | 확정 | 공지 + 사용자 실측(V4.5는 퍼센트가 안 움직임) |
| **Anlas 과금 V5**(1MP 초과·28스텝 초과)는 할당량을 **안 쓴다** | 강함 | 구간 안 과금 103장. 과금 항을 따로 맞추면 계수가 무료의 약 3% |
| 소모는 **픽셀 수에 비례** | 중간 | 1024×768 19장이 비례 예측과 일치. 1MP 미만 표본은 그뿐 |
| 스텝은 **고정분 약 19~25% + 비례분** | 강함 | 16/18/19/25스텝 직접 측정이 예측과 ±3% |
| 화면 퍼센트는 **반올림** | 확정 | 100%에서 0.5%p 쓴 뒤 99, 짧은 유휴 상승 2번이 계산과 정확히 일치 |
| 잔량이 떨어진 **직후**의 실제값은 경계 바로 아래 | 확정 | 측정의 기초. 끝점 오차 = 한 장 분량 |
| 가만히 있을 때의 **상승은 늦게 찍힌다** | 확정 | 100분 유휴 뒤 상승이 90분 늦게 찍힘 → 상승은 측정에 안 씀 |

**식에서 이것만 바꾸면 됩니다** — `overlay.js` 맨 위 `UNIT` · `PIXEL_EXP` · `STEP_FIXED`.
환산은 `unitsPerImage` 한 곳이고 예약 창도 `NibUsage`로 같은 것을 씁니다.

---

## 아직 모르는 것

| 모르는 것 | 영향 | 확인하려면 |
|---|---|---|
| 고정분이 픽셀에 비례하나, 장당 고정인가 | 1MP 미만 생성만(최대 약 6%) | 640×960 같은 작은 크기로 한 칸(1%p) 이상 |
| 스텝 식이 "고정분+비례분"인가 "스텝^0.7"인가 | 16~25스텝에선 차이 ≤2%. 28스텝·10스텝 같은 끝에서만 갈림 | 28스텝·10스텝으로 각각 한 칸 이상 |
| 여러 장 요청(Number of Images ≥ 2)의 할당량 | 예약 큐는 늘 한 장씩이라 영향 없음 | 4장 요청을 한 칸 이상 |
| 샘플러·Variety+ 같은 설정이 소모를 바꾸나 | 모름. 원장에 **기록하지 않음** | `page.js`에서 `sampler` 등을 함께 기록하도록 확장 |
| img2img · 인페인트 · 바이브 | 크기·스텝대로 셈(보정 근거 없음) | 해당 기능으로 한 칸 이상 |
| 캐릭터 레퍼런스 | **V5에 아직 없음**(V4.5 전용). 생기면 A1과 같다고 가정 | V5에 기능이 생긴 뒤 |

---

## 실측 다시 하기

**한 칸 방법** — 가장 적은 장수로 한 설정을 재는 법입니다.

1. 평소 설정으로 생성하다가 화면 퍼센트가 **한 칸 떨어지는 순간**(예: 90 → 89)을 봅니다.
2. 곧바로 잴 설정으로 바꾸고, **다음 칸이 떨어질 때까지만**(89 → 88) 생성합니다. 약 20장.
3. 원래 설정으로 돌아갑니다.
4. 사용량 창 `기록 내보내기` → 다운로드 폴더의 JSON을 분석합니다.

```bash
node tools/analyze-ledger.js "C:/Users/<you>/Downloads/nib-ledger-YYYYMMDD-HHMMSS.json"
```

**주의** — 잔량이 **99% 이하**일 때 하세요. 100%에서는 소모가 흔적을 남기지 않습니다.
한 칸 사이에는 **한 가지 설정만** 섞어야 직접 측정(출력 4번)에 잡힙니다.

**출력 읽는 법** (`tools/analyze-ledger.js`)

| 번호 | 볼 것 | 이상 신호 |
|---|---|---|
| 1 | 사용량 창이 보여줄 값 | 창끼리(50/200/전체) 5% 넘게 다름 |
| 2 | 하락 구간 목록 | 소모가 음수이거나 2 이상인 구간(한 칸짜리가 아님) |
| 3 | 모델 비교 RMS · A1 검사 | 자유 맞춤 고정분이 0.19에서 ±0.1 넘게 벗어남 · 과금 계수가 10% 넘음 |
| 4 | 스텝별 실측 대 예측 | ±5% 넘는 스텝이 반복해서 나옴 |
| 5 | Anlas 검산 | 새 기록에서 −5% 밖 |

측정 한계: 구간 하나의 잔차는 **±0.04~0.07%p**(한 장 분량)까지 정상입니다. 이어진 구간은 안쪽 오차가 상쇄되어 합치면 정확해집니다.

---

## 코드에서 이 기능이 사는 곳

```
page.js        MAIN world. 생성 요청 후킹 → 크기·스텝·장수·모델 기록, Anlas 차감 측정
               beginMeasure/settle: 측정은 한 번에 하나. 충전(잔액 증가)은 null
background.js  원장의 유일한 기록자. 마크마다 회복 주기(s), repairAnlas(옛 기록 1회 정리),
               splitDoubled("앞 null, 뒤 2배"를 단가가 정확히 맞을 때만 나눔), ledger.export
overlay.js     소모 모델(quotaUnits · unitsPerImage) + 추정(analyze) + 사용량 창
               dropAnchors → pickAnchored(세션을 넘어 모음) → 오염 제외(badSpans)
queue.js       예약 창 Opus 예측 = 기준 1장당 × 베이스 기준 장 × 장수 (NibUsage 사용)
tools/         analyze-ledger.js — 내보낸 원장 분석(확장에는 안 실림)
```

---

## 알려진 한계

- 화면의 오차(±N%)는 **측정 오차만**입니다. 식 자체의 오차는 스텝별 대조로 약 ±3%.
- **Anlas 차감 기록**: 과금을 빠르게 연달아 누르면 차감이 다른 칸에 찍힐 수 있습니다. 합계는 보존되지만
  개별 값이 틀릴 수 있고(`—`, `−64`), 합계도 예상보다 약 3% 적게 잡힙니다. 할당량 계산은 이 값을 안 씁니다.
- 2026-09-23 이전 원장의 Anlas 기록에는 옛 버그 흔적("무료인데 45")이 남아 있습니다. 정리 대상이 아니라 그대로 둡니다
  (할당량 계산에 영향 없음, Anlas 합계만 부풂).
- 마크에 회복 주기(`s`)가 없는 옛 기록은 지금 값(6048)으로 계산합니다. 7888 시절 기록이면 그 구간은 틀립니다.

---

## 되돌리기

- **전부**: 확장 폴더를 `NovelAI V5 Nib.backup-2026-09-23`으로 바꾸고 ↻ + F5.
- **식만**: `overlay.js`의 `STEP_FIXED = 0` → 예전 "스텝 정비례"와 같아집니다.
- 원장 형식은 하위 호환입니다(새 필드 `marks[].s`, `gens[].ref`, `anlasFixed`는 옛 코드가 무시).
