# Nib 내부 구조

무엇이 어디에 있고 왜 그렇게 되어 있는지. 실측한 값은 전부 여기 모읍니다.
사용법은 [README.md](README.md), 고칠 때의 함정은 [MAINTENANCE.md](MAINTENANCE.md).

---

## 파일과 실행 위치

```
manifest.json     MV3 설정
background.js     사이드 패널 열기 + 사용량 원장의 유일한 기록자 (서비스 워커)
shared-ui.js      NovelAI 위 창들의 공용 바탕 — 토큰·창틀·드래그·테마·마크·칸 스키마 (격리 world)
content.js        프롬프트 조작 엔진, 릴레이, 메시지 라우터 (격리 world)
page.js           React 상태 읽기 + fetch/XHR 후킹 (MAIN world)
overlay.js        NovelAI 위 사용량 창 (shadow DOM)
editor.js         NovelAI 위 프리셋 편집 창 (shadow DOM)
queue.js          예약 생성 큐 — 베이스 고정 + 세트 순차 실행 (독립)
cleaner.js        태그 정리 창 — 가중치 문법·공백 변환기 (독립, 순수 함수 + 창)
viewer.js         출력 뷰어 평탄화 — 라운딩·호버 줌 제거 (선택, 독립)
sidepanel.html/css/js   라이브러리 UI
icons/            nib.svg 원본 + 툴바용 PNG 16·32·48·128
tools/            analyze-ledger.js — 내보낸 원장 분석 (node로 실행, 확장에는 안 실림)
HANDOFF.md        Opus 사용량 계산 인계 문서 — 상태·근거·모르는 것·실측 방법
WORKLOG-*.md      작업 일지 (시간순, 앞부분은 뒤에서 바뀐 판단 포함)
```

**`page.js`만 MAIN world입니다.** 콘텐츠 스크립트는 격리된 JS 컨텍스트라 페이지가 DOM 노드에 붙인
`__reactFiber$…`도, 페이지의 `window.fetch`도 건드릴 수 없습니다. 관측한 것은 `postMessage`로 넘깁니다.
fiber 탐색 비용 실측 **37개, 21.7ms**(5초 주기).

**편집 창은 사이드 패널이 아니라 NovelAI 화면 위에 있습니다.**

```
사이드 패널  send({cmd:'editor', item, categories, characters, charSlot})
      │        분류 목록·페이지 캐릭터 번호는 패널만 아는 값이라 실어 보낸다
      ↓
content.js 라우터 → globalThis.openPresetEditor(payload)
      ↓
editor.js  저장 = chrome.storage['nib.library']에 직접 쓰기
      ↓
사이드 패널  storage.onChanged로 듣고 다시 그린다
```

결과를 메시지로 되돌려주지 않는 이유는 **사이드 패널이 닫혀 있어도 저장이 남아야** 하기 때문입니다.

---

## 저장소

```
nib.library          프리셋 { version:2, items[], folders[] }
nib.ledger           사용량 { version:2, spp, marks[{t,p,s}], gens[{…, sig, ref}], ids[], current, anlas, anlasFixed }
nib.theme            'auto' | 'light' | 'dark'
nib.charSlot         마지막으로 고른 캐릭터 번호
nib.hideSuggestions  "Did you mean?" 숨김
nib.flatViewer       출력 이미지 모서리 라운딩·호버 줌 제거
nib.queuePlan        예약 세트 구성 [{ entries[], count }]
nib.cleanerMerge     태그 정리 — 인접 동일 가중치 병합 (기본 false)
nib.queuePos         예약 창 위치
nib.overlayPos       사용량 창 위치
nib.editorPos        편집 창 위치
nib.cleanerPos       정리 창 위치
```

상한: gens 8000 / marks 3000 / ids 400. 넘으면 오래된 것부터 버립니다.

**분석 도구** — `tools/analyze-ledger.js`(확장에는 안 실림)가 내보낸 원장으로 사용량 창 값, 하락 구간,
모델 비교, 스텝별 실측, Anlas 검산을 한 번에 찍습니다. 쓰는 법은 [HANDOFF.md](HANDOFF.md).

**원장 내보내기** — 사용량 창의 `기록 내보내기`. 서비스 워커가 `chrome.downloads`로 다운로드 폴더에
`nib-ledger-YYYYMMDD-HHMMSS.json`을 떨구고 실제 경로를 알림창으로 보여줍니다(`downloads` 권한).
페이지에서 `<a download>`로 내리던 첫 판은 NovelAI 페이지 안에서 **조용히 실패**했습니다.
원장은 `chrome.storage` 안에만 있어 이것 말고는 밖에서 읽을 길이 없습니다 — 실측 분석은 이 파일로 합니다.
기록 1만 건 ≈ 400KB. `unlimitedStorage` 권한 있음.

### 라이브러리 스키마

`version: 2`에서 `folders`가 생겼습니다. `version: 1`에는 그 키가 없으므로 없으면 빈 배열로 봅니다.

```js
folders: [{ id, name, home, open, createdAt }]   // home = 만든 탭 ('all'|'fav'|분류 id)
items:   [{ id, name, tags, multi, slots, fav, emoji, category, target,
            folderId, createdAt, updatedAt }]
```

- 프리셋은 `folderId` 하나만 듭니다. **폴더 안에 폴더는 없습니다** — 한 겹뿐이라 트리 순회가 필요 없습니다.
- `multi === true`면 `slots = { base, uc, chars[], charUCs[] }`.
  **`charUCs`는 `chars`와 짝을 이루는 배열**입니다(같은 인덱스 = 같은 캐릭터).
  이 키가 없던 시절의 프리셋이 저장소에 그대로 있으므로 읽는 쪽은 **항상 `normalizeSlots`를 거칩니다.**
  그 함수는 `shared-ui.js`에 **한 벌만** 있습니다(`NibUI.normalizeSlots`) — 편집 창과 예약 창이
  같은 것을 씁니다. 사이드 패널에만 사본이 있는데, 다른 실행 컨텍스트라 이 파일을 못 읽기 때문입니다.

**폴더가 어느 탭에 보이는가** — 세 규칙입니다.

1. `home`이 그 탭이면 비어 있어도 보인다
2. 그 탭에 보이는 프리셋을 담고 있으면 보인다 (담긴 것 중 그 탭에 걸리는 것만 안에 그린다)
3. `전체` 탭에는 다 보인다

목적은 하나입니다 — **폴더에 넣었다고 프리셋이 탭에서 사라지지 않게** 하는 것.
2번이 없으면 씬 탭에서 만든 폴더에 화풍 프리셋을 넣는 순간 그 프리셋이 화풍 탭에서 증발합니다.

**폴더를 지울 때 안의 프리셋** — 두 갈래를 창에서 고르게 합니다(`deleteFolder` → `dropFolder`).
폴더를 지우려는 뜻이 "묶음만 풀겠다"인지 "내용까지 버리겠다"인지는 밖에서 알 방법이 없고,
한쪽은 되돌릴 수 없기 때문입니다. **세는 것은 지금 탭에 보이는 것이 아니라 폴더가 담은 전부**입니다 —
화풍 탭에서 지워도 그 폴더의 씬 프리셋까지 함께 걸리므로, 화면에 보이는 수를 적으면 거짓말이 됩니다.
빈 폴더는 물어볼 것이 없어 `confirm` 한 번으로 끝냅니다.

**전체 초기화**(`openResetDialog` → `runReset`)도 갈래가 둘입니다 — 프리셋만, 또는 폴더까지.
폴더 구조를 다시 짜는 것이 프리셋을 다시 넣는 것만큼 귀찮아서입니다.
**창 안에 내보내기 버튼을 같이 둡니다** — 자동 백업은 같은 파일을 덮어쓰므로 비운 뒤 한 번만 더
저장되면 백업 쪽도 빈 파일이 됩니다. "먼저 내보내세요"라고 적어만 두면 아무도 하지 않습니다.
아무 일도 일어나지 않을 갈래는 감춥니다(프리셋이 0개면 `프리셋만 지우기`가 사라지고 제목이 바뀝니다).

### 내보내기 · 불러오기 형식

내보내기 두 갈래와 자동 백업이 **같은 꾸러미**를 씁니다(`libraryPayload()`).
불러오는 쪽이 "이건 부분 파일"이라고 구분할 일이 없어야 하기 때문입니다.

```js
{ version: 2, scope: 'all' | 'folders', exportedAt, folders[], items[] }
```

불러오기는 폴더마다 갈래가 넷입니다 — `keep`(원본 이름으로 새 폴더) · `rename`(새 이름) ·
`merge`(기존 폴더 안으로, 새 폴더를 만들지 않음) · `skip`(안의 프리셋까지 통째로 제외).
프리셋은 `이름 + 분류`가 같으면 덮어쓰고 없으면 추가합니다.

---

## NovelAI DOM — 실측값

클래스명이 난독화 해시(`sc-xxxxx`)가 아니라 의미 있는 이름이라 비교적 안정적입니다.
관련 상수는 전부 `content.js` 최상단 `SEL` 객체에 모여 있습니다.

| 대상 | 셀렉터 |
|---|---|
| Base Prompt | `.prompt-input-box-base-prompt .ProseMirror` |
| Undesired Content | `.prompt-input-box-undesired-content .ProseMirror` |
| 캐릭터 N 프롬프트 | `.prompt-input-box-character-prompts-N .ProseMirror` |
| 캐릭터 N UC | `.prompt-input-box-character-prompts-N-undesired-content .ProseMirror` |
| 캐릭터 행 | `.character-prompt-input-N` |
| 캐릭터 섹션 | `.image-gen-character-prompts` |

### 함정 넷

1. **입력란은 textarea가 아니라 ProseMirror contenteditable입니다.**
   `el.value` / `innerText` 대입은 화면에도 내부 상태에도 반영되지 않습니다.
   `focus()` → `document.execCommand('insertText' | 'selectAll' | 'delete')`만 NovelAI 내부 상태까지 갱신합니다.

2. **모든 입력란이 DOM에 두 벌씩 있습니다** (모바일 + 데스크톱 레이아웃이 동시에 마운트).
   `querySelector` 첫 번째를 잡으면 안 보이는 쪽에 써서 아무 일도 일어나지 않습니다.
   `getClientRects().length > 0 && visibility !== 'hidden'`로 걸러야 합니다.

3. **접힌 캐릭터와 비활성 탭은 `visibility: hidden`이라 `focus()`가 거부됩니다.**
   미리보기 버튼 클릭 → 탭 버튼 클릭 → 삽입 순서를 지켜야 합니다 (`ensureEditor`).

4. **미리보기 버튼은 글자가 아니라 폭으로 찾습니다.** 글자로 찾으면 칸이 빈 순간 못 찾고
   그 캐릭터가 영영 대상이 되지 못합니다.

   ```
   위/아래/활성/삭제/드래그 아이콘   각 34px   (이 순서 고정)
   미리보기 버튼                    373px     (행 전체 폭)
   ```

   기준 80px. 못 고르면 가장 넓은 것을 씁니다 — 아무것도 안 누르는 것보다 낫습니다.

### localStorage

```
imagegen-prompt                "..."  (Base Prompt, JSON 문자열)
imagegen-negativeprompt        "..."
imagegen-character-prompts     [{ prompt, uc, center:{x,y}, enabled }]
imagegen-params-<모델id>        모델별 설정
```

**읽기는 여기가 더 쌉니다** — 캐릭터 칸을 펼치지 않아도 됩니다. 전역 UC는 없어서 DOM에서 읽습니다.

---

## 출력 뷰어 — 실측값 (V5)

**출력부는 `<img>` 목록이 아니라 무한 캔버스입니다.** 이걸 모르면 CSS로 애니메이션을 끄려다
카메라를 죽여 이미지를 화면 밖으로 보냅니다.

```
.image-gen-canvas
  └ div (클래스 없음)                  ← 카메라. transform-origin: 0 0
      transform: matrix(0.911003, 0, 0, 0.911003, 522, 53.11)   ← JS가 인라인으로 찍는다
      └ .image-gen-canvas-tile         sc-f6314b86-1 = position:absolute만. 라운딩 없음
          └ div                        sc-f0ebfba1-22 → .fHbPDq / .eacbov (해시, 빌드마다 바뀜)
              └ img.image-grid-image   ← 라운딩은 여기
```

걸려 있던 규칙입니다. **부모는 해시라 못 쓰고 자식의 이름은 안 바뀝니다** — 그래서
`viewer.js`는 자식 클래스만 잡습니다(카메라 div는 클래스가 없어 절대 안 걸립니다).

```css
.fHbPDq > img { border-radius: 8px; overflow: hidden }
.fHbPDq > img, .fHbPDq > .image-grid-generating {
  transition: transform 0.25s cubic-bezier(0.22, 1, 0.36, 1);
}
.eacbov > img, .eacbov > .image-grid-generating,
.eacbov > .image-grid-thumbnail-standin,
.eacbov > .image-grid-variation-backdrop      { transform: scale(0.98) }
.eacbov:hover > img, .eacbov:hover > .image-grid-thumbnail-standin,
.eacbov:hover > .image-grid-variation-backdrop { transform: scale(1) }
```

호버 줌의 정체는 **0.98 → 1.0**이고 그 위에 0.25초 커브가 얹혀 있습니다.
`.image-grid-generating`은 0.98 규칙에는 있지만 호버 규칙에는 없습니다.

**검증:** 규칙을 넣고 재니 `border-radius` 8px → 0px, `transition` → `none`.
같은 순간 카메라 `matrix(0.911003, …, 522, 53.11)`과 이미지 사각형 `758×1108 @ (543, 53)`은
**1px도 안 움직였습니다.** 호버 줌은 대조군을 따로 세워
`.eacbov > img { transform: scale(0.98) }`을 재현하고 쟀습니다 — `matrix(0.9899…)` → `none`.

### 스크롤 애니메이션·패닝·줌은 NovelAI에 이미 스위치가 있습니다

Settings → Image Generation. 셋 다 기본 꺼짐입니다.

| 화면 이름 | 설정 키 | 하는 일 |
|---|---|---|
| Simple Output Viewer | `simpleOutputViewer` | 현재 생성만, 장식 없이 |
| Reduced Motion | `canvasReducedMotion` | 카메라 트랜지션 제거 |
| Lock Output Viewer Camera | `canvasLockCamera` | 패닝·줌 비활성화 |

번들에서 확인한 게이트입니다.

```js
function yb(){ return prefersReducedMotion() || settings.canvasReducedMotion }  // 카메라 트윈 우회
function yx(){ return settings.canvasLockCamera }                              // 패닝/줌 차단
s = settings.simpleOutputViewer || windowSize.width <= MOBILE_BREAKPOINT
d = s ? 0 : recentGroups   // 0 = 지금 생성 하나만 배치 → 넘길 것이 없어진다
```

`prefersReducedMotion()`은 `matchMedia('(prefers-reduced-motion: reduce)').matches`이고
`yb()` 안에서 **매번 다시 호출됩니다.** MAIN world에서 `matchMedia`를 패치하면 계정 설정을
안 건드리고도 모션만 끌 수 있다는 뜻입니다. 안 하는 이유는 하나 — 공식 경로가 있는데
흉내를 내면 두 벌이 어긋나기만 하고, `canvasLockCamera`는 reduced-motion 우회로가 아예 없어
어차피 반쪽이 됩니다.

**그래서 `viewer.js`가 맡는 것은 라운딩과 호버 줌뿐입니다.** 그 둘만 NovelAI에 설정이 없습니다.

---

## 태그 처리

### 구분자는 쉼표와 줄바꿈 **둘 다**입니다

쉼표만 나누면 줄 끝 태그가 다음 줄 첫 태그와 한 덩어리로 읽혀 영영 안 잡힙니다.
실제 프롬프트(7줄)로 잰 값입니다.

| 태그 | 위치 | 쉼표만 | 쉼표+줄바꿈 |
|---|---|---|---|
| `pajamas` `deformed` | 줄 가운데 | 잡힘 | 잡힘 |
| `depthness` `sleep bubble` `pajamas cap` | **줄 끝** | **못 잡음** | 잡힘 |

`readEditor`가 줄바꿈을 공백으로 바꾸면 같은 일이 벌어집니다. **바꾸지 않습니다.**

넣고 뺄 때는 태그와 그 뒤 구분자를 짝으로 들고 다닙니다(`splitParts` / `joinParts`).
`splitTags → join(', ')`로 되돌리면 여러 줄로 짠 프롬프트가 한 줄로 뭉개집니다.
`execCommand('insertText')`는 줄바꿈을 그대로 넣습니다 — 왕복 실측 확인.
태그를 뺀 자리의 구분자는 앞 태그가 물려받습니다(`a, b⏎c`에서 `b`를 빼면 `a⏎c`).

### 정규화

비교는 정규화 후에 합니다 — `{}`, `[]`, `1.2::tag::`, 대소문자, 여분 공백을 벗겨냅니다.
따라서 `{blue eyes}`가 있으면 `blue eyes`를 또 넣지 않고, 토글 시 함께 빠집니다.

### 캐릭터 넣기는 세 갈래

| 고른 칸 | 동작 |
|---|---|
| 이미 들어 있음 | `remove` |
| 비었고 다른 칸에도 없음 | `add` |
| 비었지만 다른 칸에 있음 | `add` 새 칸 → `remove` 원래 칸 (**옮기기**) |

`toggle`로 넣기만 하면 원래 칸에 남아 복제됩니다. 원래 칸 비우기는 **넣기 성공 후에만** 합니다.

### 자동 성별 태그

NovelAI는 캐릭터 칸을 만들 때 성별에 따라 `girl, ` 또는 `boy, `를 미리 넣어둡니다(미지정이면 빈 칸).
**정규화로는 못 잡습니다** — `normalizeTag`는 가중치와 대소문자만 벗기므로 `girl`과 `1girl`은
끝까지 다른 키입니다. 그냥 두면 `girl, 1girl`이 남습니다.

`content.js`의 `cleanGender`가 넣기 직전에 자동으로 생긴 쪽만 걷어냅니다. 조건 셋을 모두 만족할 때만입니다.

- `target.kind === 'char' && !target.uc` — 캐릭터 UC와 Base에는 NovelAI가 이 태그를 넣지 않습니다
- `incoming.some(isGenderTag)` — `girl` `boy` `1girl` `2girls` `3boys` 등.
  없으면 지우지 않습니다. 화풍만 넣었는데 성별이 사라지면 안 됩니다
- 지우는 것은 `AUTO_GENDER`(낱개 `girl` / `boy`)뿐 — 사용자가 쓴 `1girl`은 남깁니다

비용은 재 봤습니다 — 태그 60개 칸에 프리셋 40개(실제보다 훨씬 큰 값)로 **0.018ms**.
같은 삽입 한 번의 고정 대기가 `writeEditor` 150ms + `dismissSuggestions` 30ms이므로 약 **1/10,000**입니다.

### "Did you mean?" 숨기기

클래스명이 바뀌어도 견디도록 선택자 대신 **"did you mean" 텍스트를 가진 가장 깊은 요소**를 찾습니다.
안전장치 둘 (`content.js`의 `hideSuggestionsIn`):

- `.ProseMirror`를 품은 요소와 입력란 **안쪽** 노드는 절대 숨기지 않는다
- 위로 감싸는 범위를 넓힐 때 부모의 글자 수가 급격히 늘면 멈춘다 (엉뚱한 큰 블록 방지)

---

## 태그 정리기 — 규칙과 근거

`cleaner.js`. 문자열 in, 문자열 out. `openCleaner.convert(text, { merge })` 하나가 전부이고
창은 그것을 보여주기만 합니다. 외부 통신 없음, 어휘 검증 없음, 순서 재배치 없음.

### 공식 문서에서 확인한 값

출처는 [Strengthening & Weakening](https://docs.novelai.net/en/image/strengthening-weakening/).

| 항목 | 값 |
|---|---|
| `{ }` | 초점 ×1.05 (쌍마다 곱셈 — `{{ }}` 는 1.1025) |
| `[ ]` | 초점 ÷1.05 |
| 수치 강조 | `숫자::텍스트 ::` — 숫자 **없는** `::` 가 닫기 |
| 음수 | `-1` ~ `-3`. **V4.5 이상**에서만 동작 |
| 특이사항 | `::` 는 닫히지 않은 괄호도 함께 닫는다 |
| UC 칸 | `{ }` 가 회피를 **강화**한다 |

마지막 줄이 중요합니다 — UC에서 뒤집히는 것은 "초점"이라는 **말**이지 수치가 아닙니다.
그래서 **칸에 따라 분기하지 않습니다.** 같은 입력은 어디에 넣든 같은 결과가 나옵니다.

### 순깊이 1.05^d

`{` 는 +1, `[` 는 -1로 세고 가중치는 `1.05^d`를 소수 둘째 자리에서 반올림합니다.

| 입력 | d | 실제 | 결과 |
|---|---|---|---|
| `{tag}` | 1 | 1.05 | `1.05::tag::` |
| `{{tag}}` | 2 | 1.1025 | `1.1::tag::` |
| `{{{tag}}}` | 3 | 1.157625 | `1.16::tag::` |
| `[tag]` | −1 | 0.952… | `0.95::tag::` |
| `{[tag]}` | 0 | 1 | `tag` |

**`{{{tag}}}`는 1.15가 아니라 1.16입니다.** 0.05씩 더하는 표가 흔히 돌아다니지만
곱셈이므로 3겹부터 어긋납니다. 표 대신 식을 쓰면 4겹 이상도 규칙 하나로 덮입니다.

### 왜 정규식이 아니라 스캐너인가

정규식은 중첩 깊이를 셀 수 없습니다. 그리고 쌍마다 감싸면 안 됩니다 —
`{{tag}}`를 안쪽부터 두 번 감싸면 `1.05::1.05::tag::::`가 나옵니다.
글자마다 순깊이를 세어 **같은 깊이가 이어지는 구간**을 한 번에 쌉니다.

중첩된 `::`가 서로 곱해지는지는 문서에 없어서, 구간은 **평평하게** 냅니다.

### 파이프라인 — 순서가 곧 규칙

```
0  NBSP·CRLF 정리
1  \( \) 이스케이프 해제
2  WebUI (tag:1.2) → 1.2::tag::
3  레거시 괄호 → 수치        (스캐너)
4  가중치 표기 정규화         (:: 짝 맞추기)
5  공백·쉼표 정리
6  [옵션] 인접 동일 가중치 병합
```

공백 정리가 뒤에서 두 번째인 이유는 앞 단계가 만든 얼룩까지 한 번에 쓸기 위해서입니다.

### 함정 넷

1. **공백 정리에 `\s`를 쓰면 안 됩니다.** 줄바꿈까지 먹어 여러 줄 프롬프트가 한 줄로
   뭉개집니다 — `splitTags`가 줄 끝 태그를 못 잡던 것과 같은 함정입니다.
   `[^\S\n]`과 `[ ]`만 씁니다.

2. **`:숫자`가 있을 때만 괄호를 건드립니다.** 맨 괄호까지 잡으면
   `hakurei reimu (touhou)`의 시리즈명이 가중치로 둔갑합니다.
   맨 괄호는 바꾸지 않고 **알리기만** 합니다 — `(masterpiece)`와 `(cosplay)`를
   기계적으로 구분할 방법이 없습니다. 가르는 선은 위치입니다:
   구분자 사이 토막이 **통째로** 괄호에 싸여 있을 때만 강조로 봅니다
   (danbooru의 괄호는 언제나 이름 뒤에 붙으므로 안 걸립니다).

3. **닫는 `::` 앞 공백은 앞 글자가 숫자면 못 지웁니다.** `hp 5 ::`를 `hp 5::`로 만들면
   다음 실행에서 그 `5`가 여는 가중치로 읽힙니다. 실제로 테스트가 잡아낸 버그입니다.

4. **멱등해야 합니다.** 두 번 돌려서 `1.05::1.05::tag::::`가 나오면 실패입니다.
   테스트 하네스가 모든 케이스를 두 번 돌려 확인합니다.

### 병합이 기본 꺼짐인 이유

`1.1::a::, 1.1::b::` → `1.1::a, b::`로 붙이면 구분자 `, `가 가중치 구간 **안으로**
들어갑니다. 두 형태의 출력이 같다는 근거를 공식 문서에서 찾지 못했습니다.
확인 전까지는 사용자가 켜는 스위치로 둡니다(`nib.cleanerMerge`).
붙일 때는 **같은 줄 안에서만** 붙입니다 — 줄을 넘으면 위에서 지킨 줄 구조가 무너집니다.

---

## Opus 사용량 — 원리와 한계

### 공지에서 확인된 것

출처: NovelAI 공지 *Subscription Updates: Usage Limits*(journal.novelai.net, 2025).

- 사용량 제한은 **V5에만** 걸립니다. 다른 모델은 무제한입니다.
- "Both resolution and step count affect how much of your usage limit a generation consumes."
  "cost scales with the pixel count of your chosen resolution" — **식은 공개돼 있지 않습니다.**
- 잔량은 배터리처럼 시간에 따라 계속 차오르고, 0에서 100%까지 약 일주일 걸립니다.
- 다 쓰면 Anlas로 계속 생성할 수 있습니다.

img2img · 인페인트 · 바이브 · 레퍼런스 · 여러 장 요청이 어떻게 셈되는지는 공지에 없습니다.

### 서버가 주는 값은 이게 전부입니다

```js
{ percent: 86, isNegative: false, timeUntilNextPercent: 6048 }   // React 상태 subscription.usage
```

**`percent`는 100을 넘을 수 있습니다.** V5 출시 기념으로 한 번 100%p를 얹어준 적이 있어
(30% → 130%) 상한을 100으로 가정하면 안 됩니다. 100 이상 구간은 회복분이 버려져 소모가
흔적을 안 남기므로 `badSpans`가 그 구간을 건너는 측정을 막습니다.

**`percent`는 정수입니다.** 화면이 투박한 게 아니라 클라이언트가 받는 값 자체에 소수점이 없습니다.
게이지 CSS도 `width: 86%`로 정수가 박혀 있습니다.

**`timeUntilNextPercent`는 카운트다운이 아니라 1%를 회복하는 데 걸리는 초입니다.** 이미지 10장 생성 +
퍼센트 1 감소 + 1시간 경과를 거치고도 1초도 안 움직였습니다. **값은 바뀝니다.**

```
예전   7888 → 하루 10.95%   (NovelAI 표시 "11% per day")
지금   6048 → 하루 14.3%, 0→100% 정확히 7일   ← 2026-09-23 원장. 공지의 "약 일주일"
```

그래서 background.js가 마크마다 그때 값을 `s`로 남기고, `refillBetween`이 바뀐 지점에서 끊어 더합니다.
소수부는 존재하지 않으므로 "이번 생성이 얼마 썼는지"는 원리적으로 알 수 없습니다.

### 할당량 소모 모델 — 실측으로 맞춘 식

`overlay.js` 맨 위에 모여 있습니다. 틀렸다면 그 상수만 고칩니다.

```
한 장 소모 ≈ (가로×세로 / 832×1216) × (0.0125 + 0.00196 × 스텝)   %p
기준 장    = (가로×세로 / 832×1216)^PIXEL_EXP × (STEP_FIXED + (1 − STEP_FIXED) × 스텝/28)
             PIXEL_EXP = 1, STEP_FIXED = 0.19      기준 1장(832×1216·28스텝) ≈ 0.067%p
```

| | 내용 | 근거 |
|---|---|---|
| A1 | Anlas가 나가는 V5 생성(1MP 초과 · 28스텝 초과)은 할당량을 **안 쓴다** | **실측(강함).** 아래 표. 캐릭터 레퍼런스는 V5에 아직 없음(2026-09) |
| A2 | 여러 장 요청은 무료인 첫 장만 할당량을 쓴다 | **가정.** 예약 큐는 늘 한 장씩이라 영향 작음 |
| A3 | **픽셀 수에 정비례** | **실측.** 1024×768·25스텝 19장 → 한 장 0.046~0.049. 비례 예측 0.048, 픽셀 무관 예측 0.059 |
| A4 | 스텝은 **약 19% 고정분 + 스텝 비례분**. 고정분도 픽셀에 비례 | **실측.** 1216×832·25스텝 17장 → 0.0594. 스텝 정비례 예측 0.066, 스텝 무관 0.051 둘 다 오차 밖 |

**A1의 근거 — 2026-09-22 원장.** 두 끝이 모두 "생성 직후 하락"인 구간(오차 ±0.05%p)입니다.

| 구간 | 소모 | 무료 | 과금 | "과금도 픽셀만큼" 1MP당 | **"과금은 0"** 무료 한 장당 |
|---|---|---|---|---|---|
| 20:46 → 21:28 | 1.417%p | 28 | 32 (1920×1088 등) | 0.016 | **0.0506** |
| 21:28 → 21:35 | 1.071%p | 21 | 1 | 0.049 | **0.0510** |

"과금도 깎는다"면 두 구간이 3배 어긋나고, "과금은 0"이면 일치합니다.

**A3·A4의 근거 — 하락 구간 6개(2026-09-22~23)를 한 식으로 맞춘 결과.**

| 구간 | 장수 | 설정 | 관측 소모 | 식의 예측 | 차이 |
|---|---|---|---|---|---|
| 09-22 20:46 → 21:28 | 28 | 19스텝 ~1MP | 1.417 | 1.393 | +0.024 |
| 09-22 21:28 → 21:35 | 21 | 19스텝 ~1MP | 1.071 | 1.045 | +0.026 |
| 09-22 21:35 → 21:39 | 1 | 19스텝 ~1MP | 0.034 | 0.050 | −0.015 |
| 09-22 21:39 → 23:47 | 28 | 16·18·19스텝 ~1MP | 1.275 | 1.309 | −0.034 |
| 09-23 08:57 → 08:57 | 17 | 25스텝 ~1MP | 1.009 | 1.046 | −0.037 |
| 09-23 12:52 → 12:57 | 21 | 25스텝, 1024×768 19장 | 1.055 | 1.032 | +0.023 |

차이가 모두 측정 오차(한 장 분량 ±0.06) 안입니다. 같은 구간을 "스텝 정비례"로 맞추면 25스텝 구간이 −0.087로 벗어나고,
"스텝 무관"이면 ±0.13~0.16으로 벗어납니다. 구간별 기준 1장당 값은 0.065~0.069로 모입니다
(예전 "스텝 정비례" 환산에서는 0.074 대 0.0665로 10% 갈렸습니다).

**기준 장** = 832×1216 · 28스텝 한 장(공지의 "normal resolutions and up to 28 steps" 상한).
생성마다 `quotaUnits(g)`로 몇 기준 장인지 셉니다 — 0이면 안 씀, `null`이면 크기를 몰라 기준 1장으로 셈.

### 평균 — 끝점을 "생성 직후 떨어진 순간"으로 잡는다

```
소모(구간) = (시작 퍼센트 − 끝 퍼센트) + 회복량(refillBetween)
기준 1장당 = 소모 / 구간 안 기준 장 합
```

퍼센트가 **생성 직후 한 칸 떨어진 순간**(`dropAnchors`)에는 실제 잔량이 경계 바로 아래입니다.
그런 두 순간 사이라면 끝점 오차는 **생성 한 장 분량**(±0.05%p 안팎)이고, 표시가 버림이든 반올림이든
양 끝에서 같은 쪽으로 어긋나 상쇄됩니다. 예전 방식(첫 생성 시점의 퍼센트 ~ 지금 퍼센트)은 ±1%p였습니다.

| | 끝점 오차 | 상대 오차 |
|---|---|---|
| 하락 두 순간 사이 (`anchored`) | 한 장 분량 | ≈ 1 / 구간 장수 — 20장이면 ±5% |
| 예전 방식 (대체 경로) | ±1%p | 1 / 누적 하락(%p) — 10%p여야 ±10% |

- **올라간 순간은 끝점으로 쓰지 않습니다.** 가만히 있을 때의 상승은 화면에 늦게 찍힙니다 —
  계산상 22:06에 올랐어야 할 97%가 23:03에야 찍혔습니다(탭이 뒤에 있었던 듯).
- 하락 직전 2분 안에 생성이 없으면 끝점으로 쓰지 않습니다(다른 기기에서 쓴 하락일 수 있음).
- 끝점이 과거여도 됩니다. 그래서 **잔량이 100%인 지금도 마지막 측정 구간의 값이 나옵니다.**

**세션을 넘어 모읍니다.** 100%에 닿으면 그 앞뒤는 한 구간으로 이을 수 없지만(`badSpans`), 그 앞 세션의
멀쩡한 하락 쌍은 그대로 쓸 수 있습니다. `pickAnchored`는 최근 하락부터 거꾸로 이웃한 쌍을 붙여 창 크기를
채우고, 오염을 만나면 끊었다가 더 앞에서 **새 덩어리**로 이어 모읍니다.

- 이어진 쌍은 한 덩어리입니다. 안쪽 끝점 오차는 앞뒤 쌍에서 상쇄되어 덩어리 오차는 한 장 분량 하나뿐입니다.
- 덩어리끼리는 독립이라 오차를 **제곱합**으로 더합니다.
- 예전에는 오염을 만나면 멈춰서, 매일 100%까지 차는 사용 패턴에서는 `전체`를 골라도 마지막 세션(약 20장)만 썼습니다.
  2026-09-23 원장으로 `전체` = 21장 → **116장(덩어리 3개), 상대오차 ±1.7%**.
- 기록표 옆 그래프는 실제로 쓴 구간(`spans`)의 생성만 진하게 칠합니다.

오차 표시는 **측정 오차만**입니다. 소모 모델(A1~A4) 자체의 오차는 들어 있지 않습니다.

**검증(합성 원장, 예전 방식):** 장당 0.062%를 쓰는 가상 원장(211장, 40시간)에서
전체 `0.063% ± 0.005`, 200장 `0.060% ± 0.005`, 50장 `0.057% ± 0.020` — 진실값을 오차 범위 안에서 복원.
**검증(실제 원장, 새 방식·실측 모델):** 기준 1장당 최근 50장 `0.0664`, 전체 `0.0672`(±1.7%).
**검증(2026-09-23 저녁, 구간 21개·무료 458장 + 과금 164장):**
- 과금 생성에 할당량 항을 따로 줘 맞추면 계수가 무료의 약 3% — **A1 재확인**.
- 스텝별 직접 측정(~1MP): 16 → 0.0446, 18 → 0.0467(구간 10개), 19 → 0.0505, 25 → 0.0593. 식의 예측과 ±3% 안.
  고정분 비율 최적값 0.22~0.25(±0.05) — 코드 0.19와 맞춤 품질 차이 없음(RMS 0.0416 / 0.0413 / 0.0415)이라 **바꾸지 않음**.
- 스텝 거듭제곱(r·s^β)은 β≈0.7에서 같은 품질 — 16~25스텝 범위에서는 "고정분+비례분"과 구분 불가.
- 고정분이 픽셀에 비례하는지(1MP 미만에서만 차이)는 1MP 미만 표본이 20장뿐이라 **판정 불가**. 과금 크기는 할당량을 안 쓰므로 실사용 영향은 1MP 미만 생성에 한정.
- 회복 주기를 자유 변수로 맞추면 6392초(90% 6061~7293)지만, 짧은 유휴 상승 두 번이 6048초·반올림 표시와 **정확히** 맞아(역산 98.451 / 96.476, 기대 98.45~98.5 / 96.45~96.5) 6048을 유지.
- 긴 유휴 뒤 상승은 계산보다 90분 늦게 찍힘 — 같은 기간 하락 구간 잔차는 +0.09뿐이라 숨은 소모가 아니라 **표시 지연**. 상승을 끝점으로 안 쓰는 이유 재확인.

**검증(합성, 느린 생성):** 3분에 한 장씩 130장 — 99%로 150분 머물러도 버리지 않고 `0.0673`(진실값 0.0672).

### 오염 구간 제외

잔량이 100%에 닿으면 회복분이 버려지고, 그 상태의 소모는 흔적을 남기지 않습니다.
보정으로 복구 불가라 그 구간을 **건너는** 측정은 쓰지 않습니다 (`overlay.js`의 `badSpans` · `crossesBad`).

- `p >= 100`이던 구간
- 구간 동안 **회복 − 생성 소모의 하한** > 여유분(`99.5 − p`) → 중간에 가득 찼을 수 있음
  (브라우저를 오래 꺼두면 마크가 없어 이 검사가 유일한 단서)
  - 여유분이 `100 − p`가 아니라 `99.5 − p`인 이유: **화면 퍼센트는 반올림입니다.** 100%로 꽉 찬 상태에서
    약 0.5%p를 쓴 뒤에야 99가 됐습니다(2026-09-23). p로 보일 때 실제 값은 최대 p + 0.5입니다.
  - 생성 소모의 하한(`MIN_UNIT_COST` 0.045%p/기준 장, 실측의 약 2/3)을 빼는 이유: 빼지 않으면 99%로 보이는 채
    50분만 지나도 계속 생성 중이었는데 버려집니다.

예전에는 "마지막 오염 이후"만 썼습니다. 지금은 오염 **이전**의 멀쩡한 구간도 씁니다.

**검증:** 3일 방치로 가득 찬 뒤 200장 생성 시나리오에서 오염 구간을 빼고 199장으로 `0.060%`를
추정했습니다(진실값 0.062%, 오차 3.2%). 보정 없이 계산하면 `0.86%`(오차 1287%)가 나옵니다.

### 평균에서 빼는 것

`quotaUnits(g) === 0`인 생성은 분모에서 빠지고, 기록표에 흐리게 남습니다(마우스를 올리면 이유).

- **V4.5** — percent를 전혀 안 깎는 것이 확인됨(사용자 실측).
- **Anlas 과금 V5** — A1(실측). 판정은 요청 본문의 크기·스텝·장수로 합니다.
  **잰 차감액(`cost`)은 쓰지 않습니다** — 2026-09-23 이전 page.js가 뒤 생성의 차감을 앞 생성에 찍는
  버그가 있어 옛 원장에 "무료인데 cost 45"가 섞여 있습니다(아래 Anlas 절).

예전에는 과금 생성 이전 구간을 통째로 잘랐습니다(`paidGen` → `paidUntil`). A1을 모를 때는 그게
맞았지만, 멀쩡한 무료 생성까지 거짓 cost 때문에 과금으로 잡혀 표본이 버려지고 있었습니다.

**Anlas 합계는 거르지 않습니다.** 돈은 모델을 가리지 않고 나가므로, 평균에서 뺀 생성이라고
지출까지 빼면 합계가 실제 잔액 변화와 어긋납니다.

### 무엇을 세나

```
경로  카운트: /ai/generate-image, /ai/generate-image-stream
      제외:   /ai/generate-image/request-price, /ai/generate-image/suggest-tags
              ← 파라미터를 만질 때마다 호출된다
기록  /^nai-diffusion-(4-5|5)(-|$)/          page.js — Anlas와 예약 큐 때문에 V4.5도 남긴다
집계  /^nai-diffusion-5(-|$)/                overlay.js — 할당량을 쓰는 것은 V5 뿐
                                             analyze()가 거르고, NibUsage.usesOpus 로 내보낸다

      **둘은 다른 범위다.** V4.5로 생성하면 percent가 전혀 안 줄어든다(사용자 실측).
      번들의 무료 판정식에 모델 조건이 없는 것은 *Anlas 과금* 기준이 그렇다는 뜻이고,
      Opus 할당량까지 같다는 뜻이 아니다 — 한때 둘을 하나로 보고 틀렸다.
장수  본문 parameters.n_samples — 요청이 아니라 장수를 센다
```

모델은 요청 본문 → 실패 시 React 상태 순으로 읽습니다. **본문 파싱 하나에만 기대면 안 됩니다** —
실패하는 순간 아무것도 기록되지 않습니다(실제로 겪음).

상태에서 읽을 때 **첫 번째로 만난 값을 쓰면 안 됩니다.** `model`이라는 키는 히스토리 항목 등
여러 곳에 붙어 있고 파이버 순회 순서는 보장되지 않습니다. 후보를 모두 모아 **만장일치일 때만**
채택하고, 엇갈리면 포기합니다 — 잘못 세는 것보다 안 세는 쪽이 낫습니다.
`selectedModel`(정확한 이름)을 우선하고 없을 때만 `model`을 봅니다. 모델을 못 읽으면 세지 않습니다.

NovelAI의 **Always Show Usage Limit Bar 설정과는 무관합니다.** 실측 확인: 설정을 끄면 화면 막대는
사라지지만(`bannerVisible: false`) 같은 순간 `usage`는 그대로 읽혔습니다.

### 원장이 꼬이지 않게 한 장치

`chrome.storage`는 읽기-수정-쓰기가 원자적이지 않아, NovelAI 탭이 둘이면 기록이 조용히 사라집니다.

1. **쓰기는 서비스 워커 한 곳** (`background.js`의 `withLedger` 프로미스 체인).
   콘텐츠 스크립트는 메시지만 보냅니다. 워커는 프로필당 하나이므로 직렬화하면 경합이 사라집니다.
2. **생성마다 UUID** — 최근 400개 id로 중복 무시. 재시도해도 안전합니다.
3. 워커가 잠들어 첫 메시지가 떨어질 수 있어 **3회 재시도** (`content.js`의 `relay`).
4. **2xx 응답만** 카운트.
5. 퍼센트 마크는 **값이 바뀔 때만** 남깁니다(회복 주기 `s`가 바뀔 때도). 5초마다 쌓지 않습니다.
6. 시계가 미래이거나 7일 이전이면 현재 시각으로 대체합니다.
7. 한 번의 실패가 이후 기록을 막지 않습니다 — 체인은 항상 성공 상태로 이어집니다.

---

## Anlas

```js
subscription.trainingStepsLeft = { fixedTrainingStepsLeft, purchasedTrainingSteps }
```

화면의 잔액 = **두 값의 합, 정수**. `subscription.usage`의 형제 키라 `page.js`가 이미 도는
파이버 순회에 얹어 함께 읽습니다(실측 **20.9ms**, 5초 주기 — 순회가 늘지 않습니다).

무료 판정식은 번들에서 그대로 가져온 NovelAI 원문입니다.

```js
!characterRef && width * height <= 1048576 && steps <= 28
```

실측: 832×1216 = **1,011,712px** · 스텝 16 → 무료.
**정사각 1024가 기준이 아니라 픽셀 수가 기준입니다** — 세로를 조금만 늘려도 과금 구간입니다.

**이 식은 이미지 한 장 기준입니다.** 판정식에 장수가 없는 이유는 장당으로 매기기 때문이고,
Opus 무료는 **요청당 한 장**까지입니다. 실측 — 같은 설정으로

| 요청 | 차감 |
|---|---|
| 1장 | **0** |
| 4장 | **−60** (첫 장 무료, 나머지 3장 × 20) |

그래서 `expectFree`는 장수를 함께 보고, 예약 큐는 **절대 묶어 보내지 않습니다.**

### 차감액은 재는 것이지 계산하는 것이 아닙니다

생성 전후 잔액 차이를 `gens[].cost`에 적습니다. Anlas는 회복이 없고 정수라 **델타가 곧 정답**이고,
`usage.percent`에서 쓴 평균 추정이 필요 없습니다. 측정 지점 **1.5s / 6s / 15s** — 먼저 바뀌면 거기서 멈춥니다.

**측정은 한 번에 하나만 열려 있습니다** (`page.js`의 `pending` · `beginMeasure` · `settle`).
예전에는 생성마다 탐침을 따로 걸어, 무료 생성 뒤 10초 안에 과금 생성을 누르면 뒤 생성의 차감이
앞 생성의 15초 탐침에 걸렸습니다 — **무료 생성에 −45, 과금 생성에도 −45가 찍혀 두 번 셌습니다**
(2026-09-22 원장: 1216×832 무료 13건이 cost 45). 지금은 다음 요청이 나간 **직후** 잔액을 새로 읽어
이전 측정을 닫습니다. 그 순간의 잔액에는 새 요청의 차감이 아직 들어올 수 없으므로, 달라졌다면
이전 생성의 몫입니다. 같은 값이 새 생성의 `before`가 됩니다(예전에는 5초 묵은 캐시였습니다).

남는 틈: 과금 생성의 차감이 화면에 반영되기 **전에** 다음 생성을 누르면 그 차감이 다음 생성에 찍힙니다
("앞 null, 뒤 2배" — 2026-09-23 원장에서 1024×1536 32 Anlas가 null → 64). 할당량 평균은 `cost`를 쓰지 않으므로 영향이 없습니다.

- **짝 나누기** (`background.js`의 `splitDoubled`) — 앞이 null(과금 예상), 뒤 값이 두 생성의 **단가 합과 정확히 같을 때만**
  단가대로 나눕니다. 단가는 같은 서명(w×h×steps)에서 가장 흔한 값이 3건 이상·과반일 때만 인정합니다. 원장 재생: 21건 교정, 합계 불변.
  두 칸 건너 찍히거나 순서가 뒤집힌 경우는 짝을 확정할 수 없어 그대로 둡니다.
- **충전** — 잔액이 늘면(구매·월 지급) 차감이 아니므로 `null`. 예전에는 `−9918`처럼 음수로 적혀 합계를 망쳤습니다.
- 그래도 합계는 예상(단가 × 장수)보다 약 2.6% 적게 잡힙니다(충전과 겹친 차감 등, 화면에 따로 안 뜬 변화).
- **옛 기록 한 번 정리** (`repairAnlas`) — 위 두 규칙은 새로 들어오는 값에만 걸리므로, 이미 쌓인 음수와
  "null/2배" 짝은 화면에 남아 있었습니다(`−-9918`, 실제 Chrome에서 확인). 서비스 워커가 원장을 처음 열 때
  같은 규칙을 시각순으로 한 번 돌리고 `anlasFixed = 1`을 남깁니다. 원장 기준 21건 교정, 합계 불변, 3ms.
- 표시: 음수는 `충전`, 못 잰 것은 `—`.

판정식은 검산용입니다. 끝내 잔액이 안 바뀌면 **무료 예측이 참일 때만 0**, 아니면 `cost: null`(모름)입니다.
`0`과 `null`을 같게 다루면 합계가 조용히 틀어집니다.

### 함정 둘

- **이미지가 붙는 생성(i2i·레퍼런스)은 `multipart/form-data`로 나갑니다.** 본문 JSON이 `request` 칸의
  Blob에 들어 있어, 문자열 본문만 보면 장수와 크기를 통째로 놓칩니다.
- `/ai/generate-image/request-price`는 번들에 **정의만 있고 호출부가 없습니다.** 서버는 가격을 주지 않습니다.


---

## 예약 생성 큐

얼려둔 베이스 위에 세트마다 다른 프리셋을 얹어 순차 생성합니다. `queue.js` 한 파일이고
`content.js`의 `globalThis.NibEngine`(`readState` · `applyTags` · `splitTags` · `normalizeTag` · `visibleAll`)과
`overlay.js`의 `globalThis.NibUsage`(`analyze` · `usesOpus`)만 씁니다.

**API를 직접 부르지 않고 Generate 버튼을 클릭합니다.** 이용약관 §9.1.6은 자동화 자체가 아니라
*한도를 무시하거나 과부하를 주는* 자동화를 금합니다. 요청을 손으로 조립하면 "클릭 대행"이라는 근거가
사라지고, NovelAI가 붙이는 파라미터를 흉내 내야 해서 틀리기도 쉽습니다.

```
동시 요청   언제나 1건
딜레이      완료 시점부터 3s + 지터 0~1s   (시작 기준 고정 주기는 서버가 느릴 때 요청이 겹친다)
재시도      없음 — 실패하면 즉시 중단
배칭        없음 — 언제나 한 장씩 (묶으면 첫 장 말고 전부 과금된다)
딜레이      완료 후 0.4~0.9초. 생성 자체가 장당 4.5초라 간격은 그쪽이 정한다 —
            딜레이는 겹치지 않게 하는 장치이지 속도 조절 장치가 아니다
중단 조건   비2xx · Opus 10% 미만 · Anlas 부족 · 워치독(예상×3, 최소 90초) · 60분 · 사용자 취소
```

### 실측값

| | |
|---|---|
| 장당 생성 | `image-gen-ms-per-step-megapixel`(290.3) × steps × 메가픽셀 = **4.48초** (832×1216·16스텝) |
| 이미지 1장 | 1.55MB · 200장 ≈ **310MB** (할당량 10,247MB의 3%) |
| JS 힙 | 69.6MB / 상한 4,192MB — **이미지는 힙에 없다** |
| Generate 버튼 | `.image-gen-generate-button` · **두 벌 마운트**(모바일+데스크톱) |
| 큐가 도는 모델 | V4.5 · V5 의 full · curated (+ inpainting) |
| Opus 예측 | V5 무료 크기일 때만. V4.5 베이스·과금 크기(가정 A1)면 `0%`라고 못박는다 |
| 장수 조절 | `.image-gen-image-count` 안의 `1`~`4` 버튼 |

이미지는 IndexedDB(`generated-images`, 키 `<세션UUID>:<이미지id>`)로 가고 캔버스는 화면 밖 타일을
컬링합니다. 그래서 **200장은 기술적 한계가 아니라 정책**입니다 — 무인 25분을 경계로 잡았습니다.

**NovelAI의 `imageAutoDownload` / `persistImageGenHistory`는 읽을 수 없습니다.** jotai 모듈 스코프에
있어 파이버 순회로 안 잡힙니다(566ms를 태우고 실패). 그래서 상태를 띄우지 않고 50장 초과 시 안내만 합니다.
세션이 바뀌면 이전 세션 이미지는 UI에서 사라지므로(실측: 45장 → 4장) 긴 큐 전에는 자동 다운로드가 필요합니다.

### 세트 전환

**앞 세트를 `remove`로 되돌리지 않습니다.** 바닥을 다시 깔고 그 위에 올립니다.

```
1. 베이스 복원   지금 값이 베이스와 다른 칸만 replace      ← 같은 칸을 다시 쓸 이유가 없다
2. 프리셋 주입   add (toggle 아님 — toggle은 있는 태그를 빼버린다)
3. 되읽어 검증   하나라도 어긋나면 생성하지 않고 전체 중단
4. 생성 반복     남은 장수를 4장씩
5. 끝            아무것도 되돌리지 않는다. 다음 세트가 1에서 다시 깐다
```

`remove`로 되돌리면 세 가지가 조용히 틀어집니다 — 두 세트가 공유하는 태그까지 빠지고, NovelAI가 자동으로
넣는 `girl`/`boy`가 판정에 섞이고, 한 번 실패하면 그 뒤 모든 세트가 오염된 채 진행됩니다.

큐가 끝나거나 멈추면 **베이스와 장수를 원래대로 되돌립니다.**

### 완료 판정

신호 **둘 다** 봅니다. `page.js`가 후킹한 2xx(`nib-gen`), 그리고 Generate 버튼이 쉬는 상태로 돌아오는 것.
응답만 보면 이미지가 앉기 전에 다음 클릭이 나가고, 버튼만 보면 실패를 성공으로 셉니다.
버튼은 `disabled`뿐 아니라 **첫 낱말이 시작 시점과 같아졌는지**까지 봅니다 — NovelAI가 버튼을 잠그는 대신
취소 버튼으로 바꿔치면 `disabled`만 보고 누르는 순간 취소를 눌러버립니다.

### 예측

Opus는 `NibUsage.analyze`를 그대로 씁니다. 평균은 **기준 장당**이라 베이스의 크기·스텝을
`NibUsage.unitsPerImage`로 기준 장 수로 바꿔 곱합니다. 베이스가 Anlas 과금 크기면 가정 A1에 따라 `0%`입니다.

```
예상 소모     = 기준 1장당 × 베이스 기준 장 수 × 총 장수
상대오차      = 소모 오차 / 소모         하락 끝점: ≈ 1/구간 장수 · 예전 방식: 1/누적 하락
추가 필요 장수 = 기준 장 toTarget ÷ 베이스 기준 장 수    ±10%가 실용 하한
```

**`toTarget`은 `analyze()`가 기준 장 수로 돌려줍니다.** 사용량 창과 예약 창이 같은 문장을 쓰므로
계산은 한 곳에만 둡니다 — 두 벌로 두었을 때 한쪽만 0 나눗셈을 막아 예약 창에 `Infinity장`이
찍힌 적이 있습니다. `avg`가 0 이하인 구간(그 사이 회복이 소모보다 컸다)에서는 `null`입니다.

Anlas는 베이스가 얼어 있어 크기·스텝이 고정이므로 계산입니다. 무료 조건이면 `0`을 단정하고, 과금 구간이면
**서명(`w×h×steps`)이 같은 표본**의 중앙값만 씁니다. 표본이 없으면 숫자를 만들지 않고, 첫 장의 실측
단가로 남은 장수를 못 채우면 그 자리에서 멈춥니다.

무료 판정(`isFreeSize`)은 **크기와 스텝만** 봅니다. `page.js`의 `expectFree`가 함께 보는
캐릭터 레퍼런스는 요청 본문에만 있어 얼려둔 베이스에서는 읽을 수 없습니다 — **V4.5에서** 레퍼런스를 붙이면
`무료 구간`이라고 적혀 있어도 Anlas가 나갑니다(V5에는 레퍼런스 기능이 아직 없습니다).
`NibUsage`가 없으면(overlay.js 실패) 판정을 포기하고 과금 쪽으로 적습니다. 그래서 이 표시는 참고일 뿐이고, 실제로 막아서는 것은
잔액을 되읽는 `guardStop`입니다.

---

## 디자인

### 토큰

색은 하나의 색상환 각도(**245**, 심야 남보라)에 중성색 전체를 묶고, 강조 하나(**양피지 금**)만 둡니다.
색을 의미 전달에만 쓰기 위해서이고, 분류 여섯 갈래가 유일한 예외입니다.

| 갈래 | 토큰 |
|---|---|
| 표면 | `--canvas` `--shell` `--surface` `--raised` `--sunken` |
| 선 | `--hairline` `--hairline-2` |
| 글자 | `--ink` `--ink-2` `--ink-3` |
| 강조 | `--accent` `--accent-hi` `--accent-soft` `--accent-line` `--accent-on` |
| 그림자 | `--shadow-s` `--shadow-m` `--shadow-l` `--inner-hi` |
| 강조가 드리우는 빛 | `--glow-accent` `--glow-accent-s` `--glow-star` |
| 반경 | `--r-shell` 16 · `--r-core` 12 · `--r-card` 10 · `--r-ctl` 8 · `--r-chip` 6 |

동심원 반경으로 이중 베젤(바깥 껍데기 + 안쪽 코어)을 만들고, 움직임은 전부
`cubic-bezier(0.16, 1, 0.3, 1)`을 씁니다. 기본 `ease-in-out`은 쓰지 않습니다.

분류색은 `--c-dim`(비활성) / `--c-hot`(적용 중) 두 변수로 카드에 들어갑니다.
폴더는 카드와 헷갈리지 않게 생김새를 반대로 잡았습니다 — 카드는 흰 표면에 분류색 띠와 그림자,
폴더는 가라앉은 바탕에 띠도 그림자도 없음.

`--glow-*` 셋이 따로 있는 이유는 하나입니다. 밝은 테마에서 강조 요소의 그림자는 **강조색 자체**가
만듭니다(흰 바탕 위에서 따뜻하게 번짐). 같은 브라스 색을 어두운 배경에 그대로 깔면 그림자가 아니라
탁한 후광이 됩니다. 그래서 어두운 테마에서는 세 토큰 모두 **배경색 기반**으로 다시 정의합니다.
쓰는 곳은 `.accent-btn` · `.seg button[aria-selected]` · `.star-btn.on` 셋뿐입니다.

### 대비 하한선 — 잰 값

**본문 글자는 놓인 표면이 무엇이든 4.5:1을 넘깁니다.** 표면이 다섯이라(`--canvas` `--shell`
`--surface` `--raised` `--sunken`) 가장 나쁜 조합을 기준으로 잡았습니다.

| | 밝은 테마 | 어두운 테마 |
|---|---|---|
| `--ink-3` (힌트·상태줄·개수·빈 목록) | 4.59 ~ 5.40 | 4.94 ~ 6.13 |
| `--ink-2` (태그·본문) | 5.73 ~ 6.75 | 6.88 ~ 8.53 |
| 강조 버튼 글자 (`--accent-on` on `--accent`) | 4.71 | 12.4 |
| 입력란 포커스 테두리 (`--accent`) | 4.79 | 12.4 |

두 가지가 걸림돌이었습니다.

**하나. `opacity`로 글자를 묽히면 안 됩니다.** `--ink-3`은 이미 가장 옅은 잉크입니다.
거기에 `opacity`를 덧대면 다시 묽어집니다 — `.version`의 `.72`가 2.6:1까지,
`.cats .n`의 `.55`가 1.99:1까지 떨어뜨리고 있었습니다. **조용함은 잉크 단계로 냅니다.**

**둘. 분류색의 기준은 흰 바탕이 아니라 골라진 칩입니다.** `.cats button[aria-selected]`는
바탕이 자기 색 12% 틴트라 대비가 한 단 낮습니다. 밝은 테마의 `--cat-*-hot` 여섯은 그 자리에서
4.5:1을 넘도록 잡았습니다(흰 바탕 기준으로는 5.3 ~ 6.6). 어두운 테마에서는 같은 틴트가 바탕을
**밝히므로** 개수 글자만 그 상태에서 `--ink-2`로 올립니다.

포커스 표시도 같은 이유로 바뀌었습니다. `--accent-line`은 흰 바탕에서 **1.48:1**이라
사실상 보이지 않았고, 입력란의 `outline: none`이 `:focus-visible` 기본 링까지 이겨
키보드 사용자에게는 남는 표시가 없었습니다. 지금은 `--accent` 테두리로 그리고,
`outline`은 지우는 대신 **투명한 2px**을 둡니다 — 강제 색 모드(고대비)에서 브라우저가
투명 윤곽선을 시스템 색으로 바꿔 칠하기 때문입니다.

### 동작 줄이기

`prefers-reduced-motion: reduce`를 사이드 패널(`sidepanel.css`)과 창 셋(`shared-ui.js`의
`shellCSS`) 양쪽에서 받습니다. **시간만 0으로 만들면 안 됩니다** — 호버로 1px 뜨는 카드가
"순간이동"으로 남습니다. 그래서 `transform`은 따로 껐습니다.

`opacity`로만 나타나는 것(별표 버튼 · 폴더 버튼 · 폴더 조작 버튼)은 **건드리지 않습니다.**
끄면 기능 자체가 사라집니다.

### 토스트는 top layer에 있어야 합니다

`showModal()`로 연 창은 top layer에 올라가므로 `z-index`로는 절대 그 위에 못 갑니다.
설정 창 **안에서** 띄우던 안내(예시 프리셋 채우기 · 백업 폴더 연결 · 불러오기 오류 · 초기화)는
그래서 아무에게도 보이지 않았습니다. 지금은 `popover="manual"`로 같은 층에 올립니다.

닫히면 `display: none`이 되므로 페이드가 살아 있으려면 `display`/`overlay`를
`allow-discrete`로 전환 목록에 넣고, 들어올 때 모습은 `@starting-style`로 줍니다.
`showPopover()`는 이미 열려 있을 때 부르면 던지므로, 열려 있으면 **글자만 갈고 시계만 다시 잽니다**
(닫았다 열면 눈에 띄는 깜빡임이 생깁니다).

### 세로 flex 안에서 줄어들지 않게 — 실측

`.list`와 `.folder-body`는 세로 flex입니다. 자식의 기본값은 `flex-shrink: 1`이고,
`.card`는 모서리를 깎느라 `overflow: hidden`이라 자동 최소 크기(`min-height: auto`)가 0으로 떨어집니다.
**둘이 겹치면 카드가 늘어날수록 한 장씩 납작하게 눌립니다.**

240장을 넣고 잰 값입니다(패널 높이 373px).

| | 카드 높이 | `list.scrollHeight` |
|---|---|---|
| `flex: 0 1 auto` (기본) | 1.3 ~ 85.8px | 1,316 |
| `flex: 0 0 auto` (지금) | 전부 85.8px | 19,693 |

```css
.list > *, .folder-body > *, .charpick-list > * { flex: 0 0 auto; }
```

같은 이유로 `.imp-scroll > *`에도 예전부터 같은 규칙이 있습니다. **하나 고칠 때 넷을 같이 봅니다.**

### 이모지 판 — 폭은 그리드에서 거꾸로 잡았다

```
칸 32 × 10 + 틈 3 × 9 + 세로 스크롤바 8 + 안쪽 여백 9 × 2 = 373  →  .emoji-panel 376px
```

칸을 `repeat(10, 1fr)`로 두면 안 됩니다. `1fr`은 `minmax(auto, 1fr)`이라 최소 크기가 칸 내용에
끌려 올라가고, 열 열 개의 합이 폭을 넘겨 **가로 스크롤이 생깁니다**(10번째가 오른쪽으로 밀려 있던 원인).
`minmax(0, 1fr)`이라야 폭에 맞춰 눌립니다. 이모지 글리프 폭은 글꼴마다 다르므로 `overflow-x: hidden`을
빗장으로 덧댑니다. 갈래마다 개수가 달라 스크롤바가 들락거리면 칸이 들썩이므로
`scrollbar-gutter: stable`로 자리를 늘 비워 둡니다.

**측정 결과 15갈래 전부 한 줄에 10개, 가로 스크롤 없음, 칸 32.2px.**
가장 큰 갈래(동물 40개)도 네 줄 138px로 `max-height: 232px` 안에 들어가 세로 스크롤도 없습니다.

이모지는 **338개, 15갈래, 중복 없음**입니다. 한 이모지는 한 갈래에만 둡니다 —
같은 것이 두 곳에 있으면 어디서 골랐는지 잊습니다. `editor.js`의 `checkEmojiSets()`가
판을 처음 펼칠 때 한 번 훑어 중복이 있으면 콘솔에 알립니다(상수라 두 번 잴 이유가 없습니다).
소스에 한 줄 10개씩 적어 둔 것도 그리드가 10칸이기 때문입니다 — **소스의 한 줄 = 화면의 한 줄.**

**폰트는 시스템 스택 고정.** Segoe UI Variable + Consolas. 웹폰트는 오버레이가 NovelAI CSP에
막힐 수 있어 넣지 않았습니다.

### 테마

```css
:root                                    /* 밝게 (기본) */
@media (prefers-color-scheme: dark) {
  :root:not([data-theme="light"]) { }    /* 자동 */
}
:root[data-theme="dark"] { }             /* 수동 */
```

**이 순서라야 자동↔수동이 양방향으로 덮입니다.** 오버레이는 shadow DOM이라 `:host([data-theme])`로
같은 일을 합니다.

### 아이콘

**원본은 `icons/nib.svg` 하나이고 PNG는 거기서 뽑은 결과물입니다.**
Chrome은 확장 아이콘에 SVG를 받지 않습니다(MV3 `icons` / `action.default_icon`은 래스터만).

```
몸통  M9.75 3.45h4.5a2.85 2.85 0 0 1 2.85 2.85v6.85c0 .3-.07.55-.2.8L12 21.35
      l-4.9-7.4c-.13-.25-.2-.5-.2-.8V6.3a2.85 2.85 0 0 1 2.85-2.85z
숨구멍 circle cx=12 cy=8.9 r=1.32
슬릿  M12 8.9 L12 21.7        ← 숨구멍 중심에서 촉 끝까지 관통해 촉이 둘로 갈린다
타일  rect rx=5.8  fill #15151d
촉    fill #efdfb0
```

색을 CSS 변수로 빼지 않은 것도 의도입니다 — PNG는 테마를 못 따르므로 셋 다 고정색이어야 같아 보입니다.
PNG 생성은 브라우저 캔버스로 했습니다.
